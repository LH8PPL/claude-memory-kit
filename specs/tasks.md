# Tasks — core-memory-kit v0.1.0

**Status**: Draft · **Author**: Claude (Opus 4.7) + the maintainer · **Date started**: 2026-05-23

This is the **actionable implementation plan**. A numbered, hierarchical checklist of concrete coding steps that build incrementally from an empty repo to a working v0.1.0 release. The bridge between [`requirements.md`](requirements.md) (WHAT v0.1.0 must do), [`design.md`](design.md) (HOW v0.1.0 is built), and [`glossary.md`](glossary.md) (canonical definitions for every domain term).

---

## ▶ Current state — what's next (read this first)

> **This section is the single source of "what we are doing now."** No other file holds it — per [`docs/DOCUMENTATION-MAP.md`](../docs/DOCUMENTATION-MAP.md), the spine (requirements/design/tasks) is the only current state. A session that lost all memory reads this + the task list below and knows the next action. If you ever find "what's next" written somewhere else, fold it here and delete it there.

- **Shipped on npm (provenance-signed):** v0.1.2 → … → v0.3.5 → v0.4.0 (Kiro cross-agent) → v0.4.1 (now.md-roll fix) → v0.4.2 (CodeQL security) → v0.4.3 (persona-promotion redesign, Task 151) → v0.4.4 (temporal-validity engine, Task 66 + commit-proposal 150) → v0.4.5 (Cursor + agent-relative backend, Tasks 196/200/201/202) → v0.5.0 (the LEARN-LOOP Phase 1, Tasks 190–193 + the privacy screen 148, tagged 2026-07-10, D-307) → **v0.5.1 (the HARDENING patch — resumable distill + Poison_Guard side-doors + install/Windows polish, Tasks 203/204/205/206/207/213/214/215/216/219/220 + fast-follow 222; tagged 2026-07-12; full cut-gate passed incl. the E1 wedge + HG1–HG6; D-320…D-323 — latest)**. Full release history: [`CHANGELOG.md`](../CHANGELOG.md); per-version lane plan: [`docs/RELEASE-PLAN.md`](RELEASE-PLAN.md); chronological why/when: [`DECISION-LOG.md`](../docs/journey/DECISION-LOG.md) (D-323 latest).

- **▶ NEXT — the v0.5 release QUEUE (D-309, order committed, digits stamp on cut per D-157):** ~~v0.5.1~~ ✅ SHIPPED 2026-07-12 (the hardening patch above) → **▶ v0.5.2 (IN PROGRESS) = the cross-agent breadth patch (scope set 2026-07-12, D-325):** Cursor adapter (196) + Codex adapter + Kiro-surface fix (165) + MCP index freshness (218); **Task 208 (Cursor interactive gate) FLAGGED PENDING-TOKENS** (maintainer's Cursor tokens exhausted — the adapter ships, the live gate waits for refresh ~2026-07-24). → **v0.5.3** = learn-loop Phase 2 (Task 194 + 209/211/212) → **v0.5.4** = governance (96 + 210; 95 if its design gate clears). Open v0.5.x follow-ups NOT in v0.5.2 (off-theme): Task 223 (L3 label-name privacy polish), Task 224 (SonarCloud server-side crash — needs web-UI), Task 221.7 (cut-gate E3 positive-dampen script fix).

_(The per-version bullets below this line are the PRE-v0.4.1 history of this header — kept as the trail; the bullets above are the live state. Refreshed 2026-07-10 (D-311) after the audit found this header frozen at Task-164-in-flight — five releases stale in the exact section a fresh session trusts first.)_

- **✅ v0.3.3 SHIPPED 2026-06-18 — the DECISIONS.md feature COMPLETE (write + AI-recall + auto-sync).** Task 157 (Bug-1 reindex crash) · 156 (AI-recall) · 155 (tombstone recovery) · 159 (auto-sync) all shipped; 158 = Bug-2 deferred to 66→95. Verified by **cut-gate-16** (full run incl. the cold-open E1 differentiator) + a pre-tag `--scope decisions` UX fix (PR #201). Two findings deferred to v0.3.4.

- **✅ v0.3.4 SHIPPED 2026-06-19** (npm `@lh8ppl/core-memory-kit@0.3.4` provenance-signed + GitHub Release `v0.3.4`; publish.yml success). **Task 161** ✅ #206 (compression timeout → bounded transient-only retry, D-174→D-175 — measurement inverted the size-spiral premise) + **161.12** ✅ #208 (retry-rate logging) + **Task 162** ✅ #207 (update path → docs both routes + `cmk doctor` HC-9 drift-check, D-176). **Verified by a full cut-gate (cut-gate17) on the real artifact — including the E1 cold-open wedge passing clean.** One finding (persona transient soft-spot, D-177) → Task 151 (v0.4), NOT a blocker (the wedge self-heals via re-synthesis). No deferrals.

- **✅ v0.3.5 SHIPPED 2026-06-20** — Task 163 #209 (ceiling-free compress timeout + backoff, D-179). (Prior shipped lane; details in the Task 163 entry below.)

- **✅ v0.4.0 SHIPPED 2026-06-25** (npm `@lh8ppl/core-memory-kit@0.4.0` provenance-signed + GitHub Release `v0.4.0`; publish.yml success). **The cross-agent, KIRO-FIRST release (D-127).** The kit works on **THREE surfaces, all live-proven:** Claude Code, **Kiro IDE 1.0.52**, **kiro-cli V3 2.9.0** — full memory loop on each (inject + capture + observe-edit + explicit + the cross-project WEDGE), **prompt-free** (incl. Kiro IDE 1.0 skill-load). Shipped: Task 50 cross-agent seam + the Kiro stack (install/uninstall, dual-agent overlays, doctor agent-awareness, delete-guardrail, trusted-commands, valid-YAML skills, MCP auto-approve) + **Task 50.N parity campaign** (#228 prompt-capture, #229 observe-edit, #230 IDE v1 hooks, #231 v1 filenames, #232 IDE-1.0 `messages.jsonl` adapter, #233 `permissions.yaml` pre-trust) + **Task 50.O** (kiro-cli cut-blockers D-197→D-200). The live gate caught + fixed three same-day Kiro IDE 1.0 breaks (the `.kiro.hook` deprecation, the session-storage move, the Trust v2 model). **Two trust mechanisms settled:** Kiro IDE = per-workspace `~/.kiro/workspace-roots/<hash>/permissions.yaml`; kiro-cli V3 = agent-config `allowedCommands`. Full arc: DECISION-LOG D-185→D-203m. **KNOWN DEFER (not a blocker):** the kiro-cli V3 delete-guardrail (`preToolUse` doesn't fire on V3; Kiro's native shell-approval covers deletes) → **Task 166**.
  - Deferred depth problems (66 temporal-validity → 95 contradiction-resolve → 96 versioning, 151 persona, 55 error→fix) = the **v0.5 depth lane**. One-differentiator-per-minor (D-24/D-146): v0.3.x = recall/consistency polish · v0.4 = breadth (Kiro) · v0.5 = depth.

- **~~▶ IN FLIGHT~~ — Task 164: lint-clean memory output — SHIPPED 2026-06-24 (PR #222, v0.4.x); this bullet is header-history, the live state is above (D-311).** (branch `task-lint-clean-memory-output`). The kit commits memory into the user's repo; a user whose CI lints markdown (markdownlint / prettier / Super-Linter) gets **hundreds of warnings on `context/`**. PROVEN by a real Super-Linter run (2026-06-23): 1058 markdown findings, 824 in `context/`, dominated by **MD022** (blanks-around-headings, 842×) from the kit's generators + the auto-extract model's prose. The fix (the user's call): emit **lint-clean markdown by construction** — NOT relax the rules. Full read+write audit done; the SAFE path is narrow (see Task 164 sub-tasks + D-194-adjacent decision records). **Code/workflows/YAML/JSON all pass clean** (actionlint et al. find nothing — only markdown is at issue).

## How to read this file

Tasks follow Kiro convention:

- **`- [ ] N.` parent tasks** group related work (one focused area, ~1–3 PR-days each).
- **`- [ ] N.M` sub-tasks** are concrete, actionable implementation steps. Max two levels of nesting.
- **`- [ ]* N.last` (asterisk)** marks the test sub-task. Optional for MVP-only paths; required otherwise. _All tests are run by the agent (Claude), not by a human._
- **`_Requirements: FR-X; design §Y_`** on the test sub-task traces back to the spec.
- Plain bullets under any task line are implementation notes / context.
- **`- [ ] N. Checkpoint — ...`** standalone numbered items between layers gate progress. Agent runs the full test suite and confirms zero failures before moving on.

Domain terms used below (Tier, Citation ID, Frozen snapshot, Poison_Guard, etc.) are defined in [`glossary.md`](glossary.md).

## Engineering discipline

1. **Test-driven**: Write the test first, let the agent implement, verify, repeat. Small cycles, high confidence.
2. **Boundary testing** (per Ousterhout, _A Philosophy of Software Design_): Test the public interface of each module, not its internal helpers. Test the contract that survives refactors.
3. **Deep modules with simple interfaces**: When implementing, wrap related code into broad modules that expose narrow surfaces. Don't fragment cohesive logic into many shallow helpers.
4. **Incremental build**: Each task produces working, integrated code. No orphaned code. After any sub-task, you can run what exists and verify it works.
5. **Code review at strategic boundaries** (not per-PR): tests are catching unit-level bugs; reviews target what tests miss. Three layer-wide reviews at Checkpoints 11, 27, 42 via the `code-review-excellence` skill. Plus individual PR reviews on Tasks 23 (auto-extract), 24 (memory-write + Poison_Guard), 31 (MCP server) — high-risk surface. See those entries for specifics. For all other tasks, just build them normally — no per-PR review step.

## Scope of v0.1.0

| Layer | Tasks (parent numbers) | Required for v0.1.0? |
| --- | --- | --- |
| Layer 1 — Foundation | 1 – 5 | **Yes** |
| Layer 2 — Granular archive | 7 – 10 | **Yes** |
| Layer 3 — Scratchpads | 12 – 15 | **Yes** |
| Layer 4 — Hooks + skill + auto-extract | 17 – 26 | **Yes** |
| Layer 5 — Search | 28 – 31 | **Optional** (deferrable to v0.1.x) |
| Layer 6 — Cron compression | 33 – 35 | **Optional** (deferrable to v0.1.x) |
| Cross-cutting | 37 – 43 | **Yes** |
| Checkpoints | 6, 11, 16, 27, 32, 36, 42, 44 | **Yes** at the gate points that apply |

Optional layers ship if time permits; otherwise they roll forward into v0.1.x patches.

---

## Layer 1 — Foundation

- [x] 1. _shipped 2026-05-23, PR #1_ — **Repo scaffolding + `template/` skeleton (T-001)** → [archive](tasks-archive.md)

- [x] 2. _shipped 2026-05-23, PR #2_ — **`cmk` Node CLI scaffold (T-002)** → [archive](tasks-archive.md)

- [x] 3. _shipped 2026-05-23, PR #3_ — **`cmk install` cross-OS implementation (T-003)** → [archive](tasks-archive.md)

- [x] 4. _shipped 2026-05-23, PR #4_ — **CLAUDE.md loader block with versioned delimiters (T-004)** → [archive](tasks-archive.md)

- [x] 5. _shipped 2026-05-24, PR #5_ — **`canonicalize()` + ID generation (Node + Python parity) (T-005)** → [archive](tasks-archive.md)

- [x] 6. _passed 2026-05-24, PR —_ — **Checkpoint — Layer 1 (Foundation) complete** → [archive](tasks-archive.md)

---

## Layer 2 — Granular archive

- [x] 7. _shipped 2026-05-24, PR #6_ — **Per-fact file format + writer (T-006)** → [archive](tasks-archive.md)

- [x] 8. _shipped 2026-05-24, PR #7_ — **INDEX.md generation + maintenance (T-007)** → [archive](tasks-archive.md)

- [x] 9. _shipped 2026-05-24, PR #8_ — **Tombstone discipline (T-008)** → [archive](tasks-archive.md)

- [x] 10. _shipped 2026-05-24, PR #9_ — **Consolidation / merge semantics (T-009)** → [archive](tasks-archive.md)

- [x] 11. _passed 2026-05-24, PRs #10_ — **Checkpoint — Layer 2 (Granular archive) complete** → [archive](tasks-archive.md)

---

## Layer 3 — Scratchpads

- [x] 12. _shipped 2026-05-24, PR #12_ — **Bounded scratchpad writer + cap enforcement (T-010)** → [archive](tasks-archive.md)

- [x] 13. _shipped 2026-05-24, PR #13_ — **Provenance frontmatter writer + reader (T-011)** → [archive](tasks-archive.md)

- [x] 14. _shipped 2026-05-24, PR #14_ — **Seed scratchpad templates (T-012)** → [archive](tasks-archive.md)

- [x] 15. _shipped 2026-05-25, PR #15_ — **`cmk trust <id> <level>` override (T-013)** → [archive](tasks-archive.md)

- [x] 16. _passed 2026-05-25, PR —_ — **Checkpoint — Layer 3 (Scratchpads) complete** → [archive](tasks-archive.md)

---

## Layer 4 — Hooks + skill + auto-extract

- [x] 17. _shipped 2026-05-25, PR #16_ — **`hooks.json` + 6-hook scaffold (T-014)** → [archive](tasks-archive.md)

- [x] 18. _shipped 2026-05-25, PR #17_ — **`cmk-inject-context` — SessionStart hook (T-015)** → [archive](tasks-archive.md)

- [x] 19. _shipped 2026-05-25, PR #18_ — **`cmk-capture-prompt` — UserPromptSubmit hook (T-016)** → [archive](tasks-archive.md)

- [x] 20. _shipped 2026-05-25, PR #19_ — **`cmk-observe-edit` — PostToolUse hook (T-017)** → [archive](tasks-archive.md)

- [x] 21. _shipped 2026-05-25, PR #20_ — **`cmk-capture-turn` — Stop hook + `stop_hook_active` guard + spawn auto-extract (T-018)** → [archive](tasks-archive.md)

- [x] 22. _shipped 2026-05-25, PR #28_ — **`cmk-compress-session` — SessionEnd hook (T-019)** → [archive](tasks-archive.md)

- [x] 23. _shipped 2026-05-25, PR #21_ — **Auto-extract subagent + CompressorBackend Haiku impl (T-020)** → [archive](tasks-archive.md)

- [x] 24. _shipped 2026-06-03, PR —_ — **`memory-write` skill + Poison** → [archive](tasks-archive.md)

- [x] 25. _shipped 2026-05-27, PR #39_ — **Conflict queue + `cmk queue conflicts` resolver (T-022)** → [archive](tasks-archive.md)

- [x] 26. _shipped 2026-05-27, PR #42_ — **Review queue + `cmk queue review` resolver (T-023)** → [archive](tasks-archive.md)

- [x] 27. _shipped 2026-05-27, PR #43_ — **Checkpoint — Layer 4 (Hooks + auto-extract + skill) complete** → [archive](tasks-archive.md)

---

## Layer 5 — Search (OPTIONAL)

> If deferred, task 25 falls back to substring match for conflict detection; `cmk search` is unavailable.

- [x] 28. _shipped 2026-05-27, PR #44_ — **SQLite + FTS5 schema + WAL config (T-024)** → [archive](tasks-archive.md)

- [x] 29. _shipped 2026-05-27, PR #47_ — **Reindex strategy (boot / runtime / recovery) (T-025)** → [archive](tasks-archive.md)

- [x] 30. _shipped 2026-05-27, PR #48_ — **`cmk search` hybrid CLI (T-026)** → [archive](tasks-archive.md)

- [x] 31. _shipped 2026-05-28, PR #49_ — **MCP server with 6 tools (stdio transport) (T-027)** → [archive](tasks-archive.md)

- [x] 32. _shipped 2026-05-28, PR #50_ — **Checkpoint — Layer 5 (Search) complete** → [archive](tasks-archive.md)

---

## Layer 6 — Cron compression (OPTIONAL)

> If deferred, task 35 (lazy compression fallback) covers no-cron environments.

- [x] 33. _shipped 2026-05-28, PR #51_ — **Daily distill cron (T-028)** → [archive](tasks-archive.md)

- [x] 34. _verified 2026-06-03, PR —_ — **Weekly curate cron (T-029)** → [archive](tasks-archive.md)

- [x] 35. _shipped 2026-05-28, PR #53_ — **Lazy compression fallback for no-cron envs (T-030)** → [archive](tasks-archive.md)

- [x] 36. _shipped 2026-05-28, PR —_ — **Checkpoint — Layer 6 (Cron + Lazy) complete** → [archive](tasks-archive.md)

---

## Cross-cutting

- [x] 37. _shipped 2026-05-28, PR #55_ — **`cmk doctor` health checks HC-1..HC-9 (T-031)** → [archive](tasks-archive.md)

- [x] 38. _shipped 2026-05-28, PR #56_ — **`cmk import-anthropic-memory` + `cmk transcripts extract` (T-032)** → [archive](tasks-archive.md)

- [x] 39. _shipped 2026-05-28, PR #57_ — **`cmk repair` + `cmk roll` (T-033)** → [archive](tasks-archive.md)

- [x] 40. _shipped 2026-05-28, PR #58_ — **Cross-OS install CI matrix (T-034)** → [archive](tasks-archive.md)

- [x] 41. _shipped 2026-05-28, PR #59_ — **Documentation: README + INSTALL + QUICKSTART (T-035)** → [archive](tasks-archive.md)

- [x] 42. _shipped 2026-05-28, PR #60_ — **Checkpoint — Cross-cutting (excluding release) complete** → [archive](tasks-archive.md)

---

## Release

- [x] 43. _shipped 2026-05-29, PR —_ — **v0.1.0 release — version, CHANGELOG, npm publish, GitHub release (T-036)** → [archive](tasks-archive.md)

- [x] 44. _closed 2026-06-03, PR —_ — **Checkpoint — v0.1.0 released** → [archive](tasks-archive.md)

---

## Late v0.1.0 addition → v0.1.1 (re-prioritized 2026-05-28)

> **Numbering + scope history**:
>
> - 2026-05-24: Task 45 was promoted from §16 v0.1.x candidate → release blocker for v0.1.0. The numbering reflects authoring order; Task 45 was tail-appended rather than slotted after Task 23 to avoid renumbering Tasks 24-44.
> - 2026-05-28: Re-prioritized to v0.1.1. The user's autopilot sequencing is 42 → 43 → 44 → 45 — meaning v0.1.0 ships WITHOUT auto-persona, and Task 45 lands in v0.1.1. The original 2026-05-24 entry is preserved below per CLAUDE.md "Decision-trail preservation" rule.

- [x] 45. _shipped 2026-05-30, PR #83_ — **Auto-persona generation (T-014)** → [archive](tasks-archive.md)

---

## v0.2 — Automatic memory + "Claude stays consistent" (the kit's core vision)

> Formalized 2026-05-30 from [`docs/journey/v0.1.1-self-test-findings.md`](../docs/journey/v0.1.1-self-test-findings.md) §"COMMITTED ROADMAP" after the Phase-1 self-test sweep landed (v0.1.2 shipped). The user: *"this was an integral part of the kit … if it's somewhere in the future I want to be there in that future, and now."* Dependency-ordered phases; **the friend handoff can happen after Phase 2 lands** (user tier finally populates), Phase 3 is the headline value.

**Phase 2 — Automatic memory (kill every manual step).** Make capture→promotion fully hands-off.
- **Task 45 (auto-persona, optimistic auto-promote — 45.6 above)** — the friend-handoff gate. Cross-project doctrine auto-lands in the user tier.
- **Auto-drain the review/conflict queues** — the daily-distill (Task 33) + weekly-curate (Task 34) passes drain `queues/review.md` + `queues/conflicts.md` automatically (optimistic auto-promote + auto-supersede). No manual `cmk queue *` step in the common case. ✅ _Shipped 2026-05-30, PR #86: `packages/cli/src/auto-drain.mjs` (`autoDrainQueues` — review→promote, conflict→keep-old) wired into both passes (daily drains P-tier; weekly drains P + U); the manual `cmk queue *` verbs still work. Glossary: [[Auto-drain]]; decision-log D-6._

**Phase 3 — The heart: "Claude remembers its own positions and stays consistent."** The FastAPI scenario (*"2 days ago you said X was a mistake; now you say the opposite"*). Three pieces (findings doc §"v0.2 headline capability"):
1. **AI-side position capture** — capture Claude's stated positions ("recommended X", "advised against Y") as timestamped, contradiction-checkable decision-facts (today only user prefs + project facts are captured; the assistant's own positions are not).
2. **Recall the dialogue** — index `context/transcripts/{date}.md` for search (today transcripts are captured to disk but NOT indexed and NOT injected) + inject a compact "recent decisions" digest at SessionStart. **Subsumes Task 51** (index session-rollup + transcript files for search).
3. **Contradiction-awareness/reconciliation** — prior position in context so Claude notices a reversal and reconciles (explain the change or self-correct). Reuses the optimistic auto-promote + auto-supersede engine from Phase 2.
- **Task 55 (behavioral pattern detection)** — extends into Phase 3; detect + promote recurring working-style patterns (depends on Task 45).

_Design home: design §16.16 (auto-persona), §16.18 (temporal/validity-windows — serves contradiction reconciliation), §16.52 (behavioral patterns). Each Phase-2/3 task gets full sub-tasks + tests when its implementation PR opens, per the per-task ship cycle._

---

## Summary

| Layer | Parent tasks | Sub-tasks (incl. tests) | Checkpoints | Required? |
| --- | --- | --- | --- | --- |
| Layer 1 — Foundation | 5 (1–5) | ~25 | 1 (#6) | Yes |
| Layer 2 — Granular archive | 4 (7–10) | ~20 | 1 (#11) | Yes |
| Layer 3 — Scratchpads | 4 (12–15) | ~17 | 1 (#16) | Yes |
| Layer 4 — Hooks + auto-extract | 10 (17–26) | ~55 | 1 (#27) | Yes |
| Layer 5 — Search | 4 (28–31) | ~22 | 1 (#32) | Optional |
| Layer 6 — Cron compression | 3 (33–35) | ~13 | 1 (#36) | Optional |
| Cross-cutting | 5 (37–41) | ~28 (Task 38 expanded 2026-05-24 to cover transcripts extraction) | 1 (#42) | Yes |
| Release | 1 (43) | 6 | 1 (#44) | Yes |
| Late addition (auto-persona) | 1 (45) | 5 | — | Yes (promoted to v0.1.0 in-scope 2026-05-24) |

**45 numbered items at the top level** (37 implementation tasks + 8 checkpoints; Task 45 added 2026-05-24 as a tail-appended in-scope addition). **~185 sub-tasks** (implementation + test). Required-only path: ~55 dev-days (Task 45 estimated L). Full surface incl. optional: ~73 dev-days.

**Critical path**: 1 → 5 → 6 (checkpoint) → 7 → 10 → 11 → 12 → 13 → 16 → 17 → 18 → 21 → 23 → 24 → 26 → 27 → 37 → 41 → 42 → **45** → 43 → 44. Task 45 (auto-persona) inserts on the critical path between Checkpoint 42 and Release Task 43 despite its high task number — Task 45 depends on Task 23 (auto-extract) and must ship before the v0.1.0 release tag per the 2026-05-24 promotion decision.

## What's deliberately not in tasks.md (audit trail)

- MCP authentication token (declined 2026-05-23 as overengineering)
- `@modelcontextprotocol/sdk` named in task 31 implementation, not in design.md
- §4.1 trust hierarchy as a separate subsection (inline §4 default mapping suffices)
- Companion skills beyond `memory-write` + `bootstrap` — v0.3+
- `<ephemeral>` privacy tag — v0.1.x candidate
- External memory provider plugin slots (Honcho/Mem0/Hindsight) — v0.2
- Web viewer rich UI — v0.2
- IDE adapters (Cursor / Windsurf / Codex) — v0.2
- Cross-project search (`cmk search --all-projects`) — v0.2 _(promoted to a real trackable entry: **Task 184**, 2026-07-01, D-247 — this one-liner sat un-scoped since v0.1.0; the cold-open finding made the case for it)_

## End of tasks.md v0.1.0

44 top-level items · ~180 sub-tasks · TDD throughout · boundary tests · checkpoints between layers · glossary reference.

Each parent task ships as a PR titled `[<task #>] <description>` (e.g., `[7] Per-fact file format + writer (T-006)`) with the task's sub-task checkboxes checked off in the PR description.

---

## Tail-appended v0.1.x candidates (post-v0.1.0 release)

> Added 2026-05-28 after the user's "look into what other products do" inquiry during Task 37. Auto-install patterns are appended here rather than slotted into v0.1.0 because they require new consent infrastructure (prompt + repair-yes flag) that's out of v0.1.0 scope. Each task here is a NEW PR after v0.1.0 ships.

- [x] 46. _shipped 2026-06-10, PR #152_ — **Reshaped post-ADR-0015** → [archive](tasks-archive.md)

- [ ] 47. **→ SWEEP VERDICT 2026-07-18 (D-353): trigger FIRED unnoticed (Task 210 touched the doctor surface). Laned v0.6.x — the doctor-UX rider batch (with 48+71); per-touch re-firing is noise.** **+ D-354 addition: HC-5 gains a cron-TARGET-exists check** (the v0.5.4 rename stranded the absolute-path schtask on the dead package name for 4 nights; sentinel-presence HC-5 stayed green while HC-10 caught it — read the registered command, stat the target, FAIL with `cmk register-crons` recovery when gone). **[Trigger (D-248/D-253): the next doctor-surface touch, or recurring HC-failure user reports]** `cmk doctor --repair` — prompt-then-install for individual failed HCs
  - Estimate: M · Depends: 46
  - **Motivation**: even with `cmk install --with-semantic` opt-in at install time, users who skipped it (or whose memsearch broke) need a runtime path to fix. Currently `cmk doctor` prints the command; user runs it manually. This task adds a prompt that the user can `[y/N]` to invoke the same install command.
- [ ] 47.1 Add `--repair` flag to `cmk doctor`
  - For each failed HC with `requiresInstall: true`: prompt user via readline interface
  - Show the full impact ("Without this, X feature is disabled") before the prompt
  - On y: invoke the recovery command via spawnSync; report result
  - On n: continue with the next failed HC; record the skip
- [ ] 47.2 Add `--yes` flag for non-interactive auto-repair
  - Useful for CI/scripted environments where the user has already consented
  - Logs every install to `.locks/doctor-repair.log` NDJSON for audit
- [ ] 47.3 Wire the `promptUser` parameter that Task 37 dropped (M3 deferral)
  - `runDoctor({...promptUser})` now actively used when `--repair` is passed
  - Forward-compat hooks rot — adding the parameter alongside the actual consumer per CLAUDE.md
- [ ]* 47.4 Tests
  - Test `--repair` with mocked prompt: y triggers install, n skips
  - Test `--yes` runs all install commands without prompting
  - Test NDJSON audit-log entries for each repair attempt
  - _Requirements: forward-looking to NFR (see §16.48); design §14_

- [ ] 48. **→ SWEEP VERDICT 2026-07-18 (D-353): rides 47's verdict — laned v0.6.x doctor-UX rider batch.** **[Trigger (D-248/D-253): rides Task 47 — same doctor-surface batch, docs-only]** Promote ask-before-install rule to a proper NFR
  - Estimate: S · Depends: 46, 47
  - The "any repair requiring `pip install` / `npm install` MUST ASK the user first" rule currently lives in design §14 as an unsourced assertion. Task 37's skill-review (I1, 2026-05-28) flagged the citation drift (it was originally cited as NFR-9 which is actually "Memory poisoning defense baseline").
- [ ] 48.1 Add the NFR to `requirements.md` (next available NFR number)
  - Text: "Consent gate for system-level installs. The kit shall NEVER invoke a system-level install command (`pip install`, `npm install`, `winget install`, etc.) without explicit user consent. Consent is provided either at install-time via an opt-in flag (e.g., `cmk install --with-semantic`) OR at runtime via an interactive prompt (e.g., `cmk doctor --repair` y/N)."
- [ ] 48.2 Update design §14 citation from "design §14 amendment" to the new NFR
- [ ] 48.3 Add the rule to CLAUDE.md "Engineering discipline" binding rules
  - Section: "Critical: never auto-invoke system installers — see NFR-N"
- [ ]* 48.4 Tests
  - Test that no production code path calls `pip install` / `npm install` / `winget install` without a consent gate above it
  - Validator (`scripts/validate-install-consent.mjs`) — structural enforcement
  - _Requirements: NFR-N (the new one); design §14_

- [x] 49. _shipped 2026-05-29, PR #63_ — **Unify install — `cmk install` wires hooks + first-class `/plugin` marketplace path (T-037)** → [archive](tasks-archive.md)

- [x] 50. _built 2026-06-20, PR —_ — **Cross-agent install via `cmk install --ide <agent>` (T-038)** → [archive](tasks-archive.md)

- [x] 51. _closed 2026-07-19, PR #308_ — **→ TRIO VERDICT 2026-07-19 (D-362): CLOSE as ABSORBED — the remaining scope shipped as Task 126's…** → [archive](tasks-archive.md)

- [x] 52. _shipped 2026-06-10, PR #149_ — **Dogfood the kit on its OWN repo (T-040)** → [archive](tasks-archive.md)

- [x] 53. _shipped 2026-05-29, PR #64_ — **Package security hardening (T-041)** → [archive](tasks-archive.md)

- [x] 54. _shipped 2026-05-30, PRs #75_ — **Code-quality + coverage CI (T-042)** → [archive](tasks-archive.md)

- [ ] 55. **→ SWEEP VERDICT 2026-07-18 (D-353): stale v0.2 lane; the error→fix shape shipped via 191/194 — the REMAINDER (cross-session behavioral patterns/insight surfacing) re-scopes into Task 95's insight-surfacing facet (its ADR-0017 home).** Behavioral pattern detection + promotion — "learn how I work," not just facts (T-043)
  - Estimate: M · Depends: 45 (auto-persona) · **v0.2 — The user endorsed 2026-05-29 ("I want this feature… a clean refinement of what we have, so we just do it")**
  - **Why**: the kit captures facts (decisions/preferences/environment) and records *some* working-style as facts (`USER.md`/`HABITS.md`/`SOUL.md`, "from now on…" memories, auto-persona Task 45). This adds the refinement: auto-extract actively **detects recurring behavioral patterns** ("always wants X format", "runs tests before commit", "prefers terse replies") and **promotes** them into `HABITS.md` / the persona, instead of leaving them as scattered one-off facts. A ranking/promotion step on the existing extract→trust→scratchpad pipeline — no new storage model.
  - **Scope boundary**: this is ONLY the clean refinement (detect + promote behavioral patterns as memory). The bigger ECC-style **procedural memory** (turn patterns into actionable reusable *skills/procedures*) is a separate, larger direction — explicitly OUT of scope, evidence-gated to a later call. Don't conflate.
  - **Sketch** (design §16.52): (1) recurrence signal in auto-extract (same canonicalized observation seen N times, or an "always/usually/prefer" cue); (2) promote to `HABITS.md` / user-tier persona with a `pattern_count`/confidence field (numeric refinement of the categorical trust level — the one idea worth borrowing from ECC); (3) surface in the frozen snapshot like other high-trust memory. Composes with Task 45 (shared promotion target), trust hierarchy (§4), review queue (medium-confidence → confirm).
  - **Design input — U-Mem contrastive failure-memory + the self-diagnosis constraint (2026-07-01, [U-Mem note](../docs/research/2026-07-01-umem-autonomous-memory.md), D-251) — THIRD independent source for the error→fix type.** U-Mem (arXiv:2602.22406, NUS) types failure-memory as **Global Procedural** ("the correct workflow/recipe for this class") vs **Local Corrective** ("in context X, don't assume Y, do Z") — adopt that split for the error→fix shape (Global ≈ the `Prevention`/lesson field, Local ≈ the `Root Cause`+`Solution` pair). **The load-bearing constraint (out-of-the-box, applies to ALL of Task 55's capture):** *"you cannot self-diagnose a failure without a correct reference."* U-Mem's evidence shows naïve self-reflection on a wrong trajectory HALLUCINATES the cause — the agent lacks the knowledge to identify its own error (their MemRL baseline extracted "trivial but correct" details that missed the real flaw). **Implication for our auto-extract:** reflecting on a session ALONE (which is what we do) is structurally weak for failure-lessons; the *correct reference* in a single-user kit is usually **the user's correction** ("no, do it this way"). So the highest-value capture signal for a behavioral/error pattern is the user-correction turn — a good-vs-bad contrast, not a solo reflection. Design 55's recurrence/cue detection to WEIGHT user-correction turns as the reference trajectory, not just count restatements. _Composes with the escalate-to-user-capture idea below._
  - **Design input — error→fix as a first-class memory category (2026-06-15, [AWS AgentCore + build-slides note](../docs/research/2026-06-15-aws-agentcore-memory-and-build-slides.md)):** AWS's build-event slides ("What Do We Store: Facts / **Errors** / Research" + a `memories/errors-and-fixes/` folder) and AgentCore's custom-strategy both treat **"we hit error X → the fix was Y" as its own memory category.** The kit captures this today only as a generic `project`/`reference` fact — no error→fix TYPE with a "what was the fix when we last hit this?" retrieval affordance. Same instinct as the ruflo/memclaw dossier below (trajectory + outcome memory), but more concrete. When designing 55, evaluate an `error`/`fix` (or error-shaped `lesson`) type + the auto-extract rule that recognizes error→fix turns. **Caveat:** a new fact TYPE ripples through the enum + trust + routing + persona logic + the taxonomy in 4 files (`write-fact.mjs`, `auto-extract.mjs`, `mcp-server.mjs`, `import-claude-md.mjs`) — design deliberately, don't bolt on. Overlaps Task 66 (a fix is true until the dep changes — temporal). _Provenance: the user's AWS build-event photos + the AgentCore docs._ **SECOND (stronger) source 2026-06-15 ([project-memory predecessor note](../docs/research/2026-06-15-project-memory-skill-predecessor.md)):** the Claude skill the user USED BEFORE building this kit had `bugs.md` as a first-class file with a clean ready-made schema — **Issue / Root Cause / Solution / Prevention** (dated, chronological). Adopt that quadruple as the error→fix shape; the **Prevention** field is the durable cross-project lesson (→ HABITS/LESSONS), the rest is project-scoped. Error→fix is now confirmed by an actually-used tool, not just a slide._
  - [ ] 55.1 Recurrence/cue detection in auto-extract
  - [ ] 55.2 Promotion to HABITS.md / persona with pattern_count + confidence field
  - [ ] 55.3 Snapshot surfacing + review-queue routing for medium-confidence patterns
  - [ ]* 55.4 Tests — recurrence detection; promotion lands in the right tier; medium-confidence routes to review; over-mutation guard
  - _Requirements: US-1/US-5 (the kit's thesis, behavioral slice); design §16.52, §6 (auto-extract), §4 (trust); Task 45_

- [x] 56. _verified 2026-06-03, PR —_ — **Upgrade vitest 2.x → 4.x (+ `@vitest/coverage-v8` in lockstep)** → [archive](tasks-archive.md)

---

## Phase 3 — AI-side position consistency (the v0.2 heart) — PROPOSED, not started

> Folded into the spine 2026-05-31 from the former `docs/journey/PHASE-3-PLAN.md` per the documentation-governance restructure: a working plan is "what's next," which lives in `tasks.md`, not a separate journey file. **Status: PROPOSED — awaiting explicit go to begin.** Target scenario: a new session where _"2 days ago you said X was a mistake; now you're saying the opposite"_ — Claude knows it, owns it, reconciles. Adds **US-16** (AI-side position consistency) + **FR-31** (capture / recall / reconcile) — see [`requirements.md`](requirements.md). (Numbering note: the pre-fold plan said "US-14/FR-31"; US-14/15 were already taken, so the new story is **US-16**.) Design calls (recommended; revisit at go): positions live as `type: decision` fact files in the project tier; capture by extending the existing auto-extract pass (no new spawn); adopt the minimal §16.18 temporal slice (`shape` + `started_at`/`ended_at`), not the full retrieval classifier.

- [ ] 57. Capture Claude's stated positions as `type: decision` facts (Phase 3 / P3.1) — PROPOSED
  - Estimate: M · Depends: 23 (auto-extract), 25 (conflict-detect) · **Phase 3 go required**
  - Extend the auto-extract subagent prompt with a `POSITION:` candidate pattern (same shape as Task 45's `PERSONA CANDIDATE`): capture durable positions ("I recommend X", "X is a mistake", "don't Y") as `type: decision` fact files, project tier, `write_source: auto-extract` (→ trust medium), with the source-transcript citation + timestamp. `<private>` stripped at capture. Reuses the bi-turn temp file + `writeFact` + trust hierarchy. Name carefully — distinct from MEMORY.md `## Pending Decisions` (open questions for the user), which these are not.
  - [ ]* 57.4 Tests — position capture lands a decision-fact with citation + trust=medium; over-mutation guard
  - **🔬 PRIMARY-SOURCE GEM (deep-read 2026-06-28; D-218).** The decision JOURNAL (`decisions-journal.mjs`, Task 147/156/159, v0.3.3) is shipped, but it's the VIEW — it journals `type:project` facts from the 6 auto-extract triggers + `cmk remember`, NOT a capture of Claude's OWN positions. `auto-extract.mjs:305` auto-DEMOTES assistant-origin lines + has no `POSITION:` pattern → the capture half is genuinely absent. Two transferable findings: (a) **letta** ([`prompts/system_prompts/sleeptime_v2.py`](https://github.com/letta-ai/letta)) — its memory-edit prompt insists "do not write 'today'/'recently', write specific dates, because memory is persisted indefinitely" → a captured POSITION must carry an ABSOLUTE date so the 66/58 reconciliation+digest have a real anchor; (b) **mem0** is a NEGATIVE source ([`configs/prompts.py`](https://github.com/mem0ai/mem0) `USER_MEMORY_EXTRACTION_PROMPT`: "GENERATE FACTS SOLELY BASED ON THE USER'S MESSAGES… YOU WILL BE PENALIZED IF YOU INCLUDE… ASSISTANT MESSAGES") — the industry default REFUSES assistant capture entirely, which VALIDATES our auto-demote hedge AND confirms position-capture-as-decision-type is a genuine differentiator no surveyed product ships. _STATUS: backlog-no-version-yet — gated on an explicit "Phase 3 go" (the trio's product-theme greenlight, never given). Entry-point of the 57→58 trio (59 folded into 66); inert alone._
  - _Requirements: US-16, FR-31; design §6 (auto-extract), §4 (trust); Task 45 precedent_

- [ ] 58. Inject a "recent decisions" digest at SessionStart (Phase 3 / P3.3) — PROPOSED
  - Estimate: S/M · Depends: 57 · **Phase 3 go required**
  - New frozen-snapshot section listing recent `decision`-facts (current positions), with its own byte budget composing with the per-tier caps (design §7.1). This is what makes reconciliation possible — the prior position is in front of Claude. Indexing transcripts for search is the **transcript half of Task 51** (the session-rollup half stays deferred per Task 51's own caveat).
  - [ ]* 58.4 Tests — digest surfaces recent decision-facts within budget; composes with the §7.1 caps
  - **🔬 PRIMARY-SOURCE GEM (deep-read 2026-06-28; D-218).** DECISIONS.md is reachable PULL-only today (`search.mjs` `SEARCH_SCOPES.DECISIONS` / `mk_search scope:'decisions'`); it is NOT pushed into the frozen snapshot — `tier-paths.mjs SCRATCHPADS_BY_TIER` excludes it, so a decision that aged out of MEMORY.md's bounded window is invisible at session start until the model thinks to search (the cross-session-blind failure the kit exists to kill). **claude-mem** ([`src/services/context/ObservationCompiler.ts`](https://github.com/thedotmack/claude-mem) `queryObservations`) shows the cheap shape: `ORDER BY created_at_epoch DESC LIMIT`, with `decision` a FIRST-CLASS observation type folded into ONE token-budgeted recency timeline (no dedicated section needed) — interleave-by-recency, not a separate carve-out. So 58's "recent decisions section" might just be a recent-slice of decision-facts in the existing snapshot, with its own byte budget composing with the §7.1 12,275-byte sum. **basic-memory** confirms the negative: its `recent_activity` is PULL-via-MCP-prompt, never auto-injected (the read-past-the-README gap D-217 flagged). _STATUS: backlog-no-version-yet — Phase 3 (hard-depends on Task 57's capture) AND the byte budget is most cheaply co-designed with Task 151's injection rework (v0.4.3). If Phase 3 goes, decide separate-section vs interleave-by-recency (claude-mem's answer) at design time._
  - _Requirements: US-16, FR-31; design §7.1 (caps); Task 51 (transcript half)_

- [x] 59. _closed 2026-07-18, PR —_ — **FOLDED INTO TASK 66 (2026-06-28, D-218).** → [archive](tasks-archive.md)

---

- [x] 60. _shipped 2026-05-31, PR #93_ — **Native-auto-memory coexistence — `cmk disable-native-memory` opt-in (ADR-0011)** → [archive](tasks-archive.md)

- [x] 61. _shipped 2026-05-31, PR #90_ — **Inline cross-project promotion — auto-persona fires *now*, not weekly (the "TikTok-age" responsiveness fix)** → [archive](tasks-archive.md)

- [x] 62. _shipped 2026-05-31, PR #91_ — **Node-only hook execution — retire the bash dependency (cross-OS parity with Claude Code)** → [archive](tasks-archive.md)

---

## v0.2.0 BLOCKERS — surfaced by the 2026-05-31 live test (must land before the v0.2.0 tag)

Full diagnosis + detailed specs: [`docs/journey/v0.2.0-live-test-findings.md`](../docs/journey/v0.2.0-live-test-findings.md) (history). These task entries are the SPINE record (per DOCUMENTATION-MAP D-19 — a fix isn't real until it's a task here).

- [x] 63. _shipped 2026-06-01, PR #95_ — **Rich-capture fix (F1 — the headline).** → [archive](tasks-archive.md)
- [x] 64. _shipped 2026-06-01, PR #96_ — **Cross-project promotion section fix (F2) — unlocks wow #1 (cross-project cold-open).** → [archive](tasks-archive.md)
- [x] 65. _shipped 2026-06-10, PR #150_ — **WINNER: sqlite-vec (in the existing index) + Xenova/bge-base-en-v1.5 via optional…** → [archive](tasks-archive.md)

- [x] 91. _shipped 2026-06-04, PR #113_ — **MEMORY.md graduation — the missing 3rd shrink mechanism (the hot index write-locks without it).** → [archive](tasks-archive.md)
- [x] 92. _shipped 2026-06-05, PR —_ — **LOW-trust drops must leave a trace (G6 from the lifecycle map).** → [archive](tasks-archive.md)
- [x] 93. _shipped 2026-06-05, PR —_ — **Inject drop must be importance-aware, not tail-order (G7 from the lifecycle map).** → [archive](tasks-archive.md)
- [x] 94. _closed 2026-06-12, PR —_ — **Memory-retention architecture — load-cap (not write-cap) + universal graduation.** → [archive](tasks-archive.md)

- [ ] 95. **Dream-style re-curation — consolidate from RAW transcripts into a REVIEWABLE output, not in-place.** _v0.3; D-62, from the 2026-06-04 Anthropic Dreams primary source ([research note](../docs/research/2026-06-04-anthropic-managed-agents-memory-and-dreams.md))._ **Deferred at the v0.5.5 backlog sweep (2026-07-17, D-349): dependencies CLEARED (Task 66 shipped v0.4.4; learn-loop Phase 1–2 / Tasks 190–194 shipped through v0.5.3; the Task-179 umbrella closed into ADR-0017 — 95 consumes the loop's utility data), but the two design forks are UNRESOLVED and each materially changes the build, so this is a genuine design fork, not a mechanical build — it must be GRILLED with the user (the design-an-interface / grilling skill), not auto-built on autopilot. Named trigger (checkable): both forks decided in a grill session — (F1) review-gate-vs-auto-apply — does the re-curation produce a queued adopt-or-discard diff (the Dreams "input never modified, output reviewable" model + the SETTLED D-126 no-auto-commit line), or auto-apply with revert? and (F2) raw-transcript privacy — re-curating from RAW transcripts (the D-44 classify-over-raw lesson) feeds unscreened verbatim content to the curation LLM, a NEW unscreened input channel (Task 231 screen-then-mask covers WRITES, not this READ) — needs its own screen design. Once BOTH are decided, this lanes into the next minor. Until then it stays deferred — "ready" is a decision someone schedules, not a state it drifts into (D-248). **Trigger refreshed 2026-07-17 (the user's call): a RESEARCH-REVIEW step comes FIRST — the user holds research papers relevant to 95 not yet in the research base; ingest them to docs/research/, THEN grill F1/F2, THEN lane. The research/design phase needs no release lane (docs-only, at the user's pace).** **→ TRIGGER RESOLVED 2026-07-18 (D-352): the review ran (the user's Sleep paper + the full 11-source base re-read — [synthesis](../docs/research/2026-07-18-task-95-design-input-synthesis.md)) and the grill SETTLED all four forks: (F1) op-class split — non-destructive ops auto under the screened envelope, lossy/generative ops behind an adopt-or-discard diff; (F2) EXTEND the shared Task-216 screen + Task-70.5 source-trust tags (a dedicated screen re-creates the D-337 two-screens-drift shape — rejected on merits); (scope) facts AND the AutoMem-style self-audit in ONE lane, facts first, self-audit second on the proven envelope; (cadence) scheduled against the today→archive roll (the Sleep invariant). The needs-design GATE IS CLEARED — remaining pre-build work is ordinary mechanism design + done-criteria, no forks. Lanes at the next sweep (after v0.6.0 Day-One per D-349).** **Absorbed at the D-353 sweep:** Task 68's weekly-curate LLM scratchpad-pruning facet (thread-resolution pruning rides the re-curation pass) + Task 55's remainder (cross-session behavioral-pattern/insight surfacing — the facet 191/194 didn't ship). **→ MECHANISM DESIGN WRITTEN 2026-07-18: [design §21](../specs/design.md) — the full contract (three-stage pass, op-class split, screening/provenance, scheduling/resumability, gates, anti-scope); no forks remain.** **Done when (build-time criteria, per design §21):** ☐ the three-stage pass (deterministic floor → one batched LLM call → code-validated ops) runs over raw transcripts + corpus; ☐ AUTO-class ops (dup cleanup, event-time supersede-marks) apply under the screened envelope with per-run caps + full audit; ☐ QUEUE-class ops (merges, insights, high-trust touches, thread-pruning) land as an adopt-or-discard diff in `context/queues/recuration.md`; ☐ input+output screened via the SHARED `screenBeforeCommittedWrite` + source-trust tags (only `user-said` may auto-promote); ☐ every condensed claim keeps source pointers + a timestamps union; ☐ HC-12 regression gate: the prospective output resurrects NO tombstoned content; ☐ Task-212 stats probe before/after, self-quarantine on worsening; ☐ never-silent-skip (every skip logged with a reason) + partial-output-preserved (ADR-0020 resumable units); ☐ **automatic-path: the pass runs + banks its diff with NO manual command** (roll-scheduled/idle machinery only; a manual `cmk dream` MAY exist but no criterion depends on it); ☐ phase 2 (the schema self-audit) ships report-only into the same queue, built AFTER the fact-pass gates prove out; ☐ over-mutation + AOEP two-sided tests; live-tested on a REAL imported/organic corpus. Our `weekly-curate`/`daily-distill` mutate in place over *derivatives*. Anthropic's "dream" model is strictly better and unifies four kit tasks: re-curate from the **raw transcripts** (D-44: classify over raw, not degraded), **merge duplicates + resolve contradictions with latest-value** (F-D semantic dedup + Task 66 temporal validity), **surface new cross-session insights** (Task 55 behavioral patterns), producing a **new reviewable output** the user adopts-or-discards (never destroys the input — the D-61 no-loss invariant applied to curation). Supersedes/absorbs the in-place curate. **Concrete operation set (D-73 dive of mem0):** mem0's batched "memory manager" decides **ADD / UPDATE / DELETE / NONE** per fact given *(existing memory + new facts)* via one LLM pass — exactly the framing this re-curation pass wants (UPDATE handles "moved Postgres→SQLite" as a supersede, not a contradicting ADD; DELETE retracts; NONE skips dups). mem0's `DEFAULT_UPDATE_MEMORY_PROMPT` is a ready template. Keep it as a **batched reviewable pass** (not per-write — per-write stays our deterministic canonical-ID dedup, zero-API). **➕ SECOND ready template (Memora Fig. 6, 2026-07-03; [research note](../docs/research/2026-07-03-memora-harmonic-memory-review.md)):** their update-or-add prompt does judge + candidate-pick + **merged-value generation in ONE call** (code: `builder/memory_builder.py::upsert_memory_entry`, similarity candidates at θ=0.80); their App. F sensitivity table (0.6 → 3.4× merges, NO quality gain — over-consolidation measured) is the empirical warning for 95's merge aggressiveness knob. **➕ SCOPE EXTENSION candidate (2026-07-10 sweep; D-308): AutoMem's meta-LLM scaffold self-audit** — their outer-loop-1 has a strong model read COMPLETE traces + the memory tree and diagnose SYSTEMIC problems (an unbounded append-log forming, redundant-write patterns, recurring empty searches), regression-gated before any change is kept. 95's re-curation should consider auditing the SCHEMA/PROCESS level too, not only fact content — the inputs (audit log + recall-log + extract.log + the `context/memory/` tree) all exist ([sweep note](../docs/research/2026-07-10-memory-research-sweep.md)); Task 212's dashboard metrics are the cheap deterministic subset that ships first. **➕ stash curator references (same sweep):** bootstrap-vs-maintain modes, the dated `## Updates` contradiction-append (never overwrite), and every-skip-carries-a-reason logging — prompt-design references for 95's reviewable pass. **➕ Sleep-paradigm ordering invariants (2026-07-18, research-review paper 1; [note](../docs/research/2026-07-18-sleep-paradigm-memory-consolidation.md)):** consolidate BEFORE the fast tier rolls (schedule 95's pass against the today→archive roll — the G8 loss spot); compute→consolidate→update (validate the transfer before applying; the input preserved until confirmed — structural evidence FOR F1's review-gate); write into NEW capacity, prune the source only AFTER adoption; more consolidation stages help but a fast-churning stable tier HURTS retention (the 2nd independent over-consolidation warning, alongside Memora App. F); self-modification needs a regression gate (their dream-rejection = the AutoMem gate's sibling). _Relates F-D, Task 55/66/84, D-44, mem0 (D-73), Memora (D-266), AutoMem + stash (D-308), weekly-curate.mjs._ **RESEARCH (2026-07-21 pass, D-380):** the 36-note dreaming corpus CONFIRMS §21's contract — Anthropic's shipped Dreams API (primary-verified: session-batched 1-100/call, review-before-apply, separate output store) and every documented failure mode in the corpus map onto defenses §21 already carries; the press's granular mechanism ("100 sessions/200-line index/4 phases") is ABSENT from Anthropic's primaries and traces to an unofficial community repo — do NOT cite those numbers as prior art. 8 borrow candidates + re-confirmed rejects live in [the dreaming synthesis](../docs/research/2026-07-21-synthesis-dreaming-vs-s21.md); the memory-poisoning paper (arXiv 2607.14611: stabilization ENTRENCHES payloads; flag-but-don't-delete 96.7% persistence) is empirical support for §21.2's QUEUE class + §21.5's gates — read both notes at task start per D-375. **FOLDED INTO THE CONTRACT (2026-07-21):** four adopted items now live in §21 itself — the capped `--focus` field (§21.2), relative→absolute date normalization as an AUTO op (§21.3 list), the no-same-session-self-grading invariant (§21.4), and the **stabilization-entrenchment pre-mortem gate (§21.5, a REQUIRED done-criterion: the planted-payload fixture must exit the pass un-entrenched)**. Build-time references (not contract): graphify's dedup guards, cognee's recursive-CTE edge queries, Mnemo's two-sided metric, COG's hygiene budget.
  - **🔬 PRIMARY-SOURCE GEM + STATUS (deep-read 2026-06-28; D-218).** Confirmed: 95 is the ENGINE for the whole 66/95/97 + deferred-tails(143/144/147/68) cluster — too big to fold, others fold into IT. The gem is exactly the mem0 `DEFAULT_UPDATE_MEMORY_PROMPT` ADD/UPDATE/DELETE/NONE pass above, paired with **letta**'s detached every-N-turns background job (sleeptime). **The key differentiator the survey confirms: neither mem0 nor letta emits a REVIEWABLE output — they mutate in place. Our no-loss diff (re-curate → reviewable new output, adopt-or-discard) is OURS, no surveyed product ships it.** **Two unresolved forks (why needs-design):** (1) review-gate vs auto-apply-revertible — the D-169 no-ritual tension (a review queue the user must drain is the anti-pattern; "auto-apply + `cmk forget` to revert" is the field-standard posture we already adopted for persona); (2) raw-transcript input is a PRIVACY surface (`context/transcripts/` is gitignored — the re-curation must emit a SANITIZED output only, never leak raw transcript content into a committed file). **Hard-blocked on Task 66** (D-166: 66 before 95 — 95 consumes 66's validity-window engine). _VERDICT: needs-deeper-design; backlog-no-version-yet, sequenced AFTER 66 (v0.4.4) — likely v0.4.5+ once the two forks are grilled._
  - **🔬 SECOND READY-TEMPLATE — U-Mem's Eliminate/Consolidate/Refine/Retain maintenance prompt (2026-07-01, [U-Mem note](../docs/research/2026-07-01-umem-autonomous-memory.md), D-251).** U-Mem's appendix Fig 12 (the "Memory Refinement Expert" prompt) is a second, independently-published template for this exact batched pass — a verified primary-source alternative to mem0's ADD/UPDATE/DELETE/NONE. Its four objectives map onto our re-curation: **Eliminate** (redundancy — highly-similar memories), **Consolidate** (merge a specific into its general form into one robust unit), **Refine** (generalize imprecise phrasing to raise transferability), **Retain** (keep valuable non-redundant items untouched). Two specifics worth stealing at build time: (a) every extracted memory must **state WHEN it applies (a trigger condition)** — U-Mem enforces this in ALL its extraction prompts; adopting it in OUR auto-extract (not just re-curation) would raise fact transferability today; (b) the explicit note *"a retrieved historical memory that was irrelevant this time is not necessarily useless — evaluate by generalizability"* — a guard against over-eager DELETE. Compare mem0's vs U-Mem's prompt at build time and take the better framing (or merge). _Relates the mem0 gem above (the first template), U-Mem note, the deferred F-D semantic-dedup (gap G5 — reworded-restatement dedup), D-251._
  - **🔬 REVISIT-WHEN-BUILDING — memclaw's fingerprint-dedup-of-rejected (deep-read 2026-06-29; D-223).** A clean pattern for the re-curation pass: memclaw's Forge computes a `fp:v1:sha256(goal+domain+top-entities+step-skeleton)` per candidate and keeps a **`forge_rejected_fingerprints` cooloff table** — when the user rejects a proposed merge/insight, its fingerprint is recorded with a 30-day TTL so the SAME proposal isn't re-surfaced next run. For us (single-user, markdown): a local `rejected-fingerprints.json` (or a tombstone-class marker) keyed on the re-curation candidate's content hash, with a TTL — so a reviewable re-curation pass doesn't re-propose a dedup/merge the user already declined. Closes the "the curation keeps suggesting the same thing I said no to" annoyance. _Relates Task 180 (same fingerprint primitive), the tombstone discipline (rejected ≈ a soft-tombstone), D-223._
- [x] 96. _shipped 2026-07-16, PR #295_ — **Compliance scrub + recovery — `redact` a leaked secret/PII from ALL history, + `cmk purge --hard` (ABSORBS…** → [archive](tasks-archive.md)
- [x] 97. _closed 2026-07-18, PR —_ — **Dynamic trust — trust scores accumulate over time. FOLDED INTO TASK 151 as sub-task 151.T (2026-06-29, D-228).** → [archive](tasks-archive.md)
- [~] 98. **→ SWEEP VERDICT 2026-07-18 (D-353): stale v0.2.x lane refreshed to a named trigger — the next cli-scaffold flake under full-suite concurrency, or the next test-hardening batch.** _PARTIAL 2026-06-10: the hazard FIRED via the Task-52 dogfood — this repo became a real kit project and cli-hooks-scaffold's bin smokes (no cwd/env isolation) engaged the REAL compress pipeline against the live session buffer (caught by the Task-65 stress gate, 5/5 consistent). That file is now fully sandboxed (per-suite temp CMK_PROJECT_DIR + MEMORY_KIT_USER_DIR); the original cli-scaffold.test.js half stays open._ **Isolate the cli-scaffold stub-smoke from the REAL user/project tier (test-infra hazard; surfaced by Task 72, D-69).** [`cli-scaffold.test.js`](../tests/cli-scaffold.test.js) iterates every subcommand-group child and runs it with `placeholder` args, asserting a stub notice — UNLESS the child is in `NON_STUB_CHILDREN` (the real, wired commands, which are skipped). The hazard: the smokes run the real `cmk` bin with **no `MEMORY_KIT_USER_DIR` / `CMK_PROJECT_DIR` isolation**, so a wired MUTATING child that someone forgets to add to the skip-set runs **destructively against the real `~/.core-memory-kit`**. Task 72 hit exactly this — `cmk persona import placeholder` round-tripped the maintainer's real persona (backed up + rewritten; caught + cleaned) before the skip-set entry landed. The skip-set is the per-command fix; this task is the structural one: run the whole scaffold smoke with `MEMORY_KIT_USER_DIR` + `CMK_PROJECT_DIR` pointed at a per-test temp dir so even a real mutating command can't touch the host. v0.2.x test-hardening. _Relates D-69, the five-doors "external calls" discipline (a smoke that mutates the host isn't isolated)._
- [x] 99. _shipped 2026-06-10, PR #148_ — **Recall benchmark — give Task 65 a number to beat (D-70, from the 2026-06-05 MemPalace review).** → [archive](tasks-archive.md)
- [x] 100. _shipped 2026-06-06, PR #123_ — **`cmk-compress-session` hangs when run manually (no stdin EOF) — the SessionEnd bin blocks on…** → [archive](tasks-archive.md)
- [x] 101. _shipped 2026-06-07, PR #125_ — **Extend the manual-run-stdin-hang fix to ALL hook bins (the rest of the class Task 100 surfaced).** → [archive](tasks-archive.md)
- [x] 102. _shipped 2026-06-06, PR #124_ — **`cmk lessons promote` / `cmk forget` give an actionable error when fed a scratchpad-bullet id (the…** → [archive](tasks-archive.md)
- [x] 103. _shipped 2026-06-07, PR #126_ — **Enrich `cmk-auto-extract` so RICH fact files ride the immune Stop-hook path, not the native-vulnerable…** → [archive](tasks-archive.md)
- [x] 104. _complete 2026-06-10, PR #157_ — **capture is time-sensitive (every uncaptured turn is unbackfillable), search is not** → [archive](tasks-archive.md)
- [x] 105. _shipped 2026-06-07, PR #127_ — **Lazy-roll `now.md` → `today-*.md` on SessionStart, so rolling compression doesn't depend on a clean…** → [archive](tasks-archive.md)
- [x] 106. _shipped 2026-06-07, PR #128_ — **Close the `now.md` compress race with the file-rename pattern (§16.27 candidate #1) — surfaced by Task 105…** → [archive](tasks-archive.md)
- [x] 107. _shipped 2026-06-07, PR #129_ — **Finish the Task 82 scaffold-cruft scrub (the CLAUDE.md block it missed) + make the `.gitignore`…** → [archive](tasks-archive.md)

## v0.2.3 — patch lane: cut-gate5 sweep findings (the wedge polish that makes it honest)

> Surfaced by the 2026-06-07 cut-gate5 full live run + CLI sweep. v0.2.2's ★ headline (B9/D1/E1) passed; these are the fixes the sweep — and the user's "no code-word passes; a regular user expects everything automatic" push — surfaced. ALL slated **v0.2.3** (patch: fixes/polish to the wedge, no new differentiator). Lane assigned in [`RELEASE-PLAN.md`](../docs/RELEASE-PLAN.md). Decision trail: D-81 / D-83 / D-84.

- [x] 108. _shipped 2026-06-08, PR #130_ — **Unify CLI + MCP over ONE shared memory-op core + RESILIENT structured input + a parity guard (the D-82…** → [archive](tasks-archive.md)
- [x] 109. _shipped 2026-06-08, PR —_ — **Fix `cmk register-crons` on Windows (broken since v0.1 — never tested).** → [archive](tasks-archive.md)
- [x] 110. _shipped 2026-06-08, PR —_ — **`cmk forget` (and every mutation) must auto-propagate — no manual `cmk reindex`.** → [archive](tasks-archive.md)
- [x] 111. _shipped 2026-06-08, PR —_ — **`cmk persona generate` errors on a real run.** → [archive](tasks-archive.md)
- [x] 112. _shipped 2026-06-08, PR —_ — **`cmk weekly-curate` — real verification.** → [archive](tasks-archive.md)
- [x] 113. _shipped 2026-06-08, PR —_ — **`cmk queue review` / `cmk queue conflicts` — real verification.** → [archive](tasks-archive.md)
- [x] 114. _shipped 2026-06-08, PR —_ — **`cmk import-anthropic-memory` — real verification.** → [archive](tasks-archive.md)
- [x] 115. _shipped 2026-06-08, PR —_ — **Transcript temp-file hygiene.** → [archive](tasks-archive.md)
- [x] 116. _shipped 2026-06-08, PR —_ — **Coverage hardening — make "never tested since v0.1" impossible.** → [archive](tasks-archive.md)
- [x] 117. _shipped 2026-06-08, PR —_ — **Free-speech intent → action coverage — make the kit INVISIBLE (the D-85 principle).** → [archive](tasks-archive.md)
- [x] 118. _verified 2026-06-08, PR #131_ — **Resolve R2 / D-80 — the `cd`-compound permission prompt — via the MCP-first surface (with / after Task 108).** → [archive](tasks-archive.md)
- [x] 119. _shipped 2026-06-09, PR —_ — **CI: bump Node-20 GitHub Actions ahead of the runner deprecation.** → [archive](tasks-archive.md)

- [x] 120. _shipped 2026-06-09, PR #145_ — **Remove the stale `memsearch + Milvus` scaffolding (the reconsidered Layer-5b backend).** → [archive](tasks-archive.md)

- [x] 122. _shipped 2026-06-09, PR —_ — **🔴 Name-privacy: extend the D-51 de-id sweep to CODE + scripts (v0.2.3 CUT-BLOCKER).** → [archive](tasks-archive.md)

- [x] 121. _shipped 2026-06-09, PR —_ — **MCP↔CLI staleness + contract + doc-currency sweep (the D-102 audit fixes).** → [archive](tasks-archive.md)

- [x] 123. _shipped 2026-06-09, PR #144_ — **cut-gate7 live-run fixes (pre-tag blockers from D-103).** → [archive](tasks-archive.md)

- [x] 126. _shipped 2026-06-10, PR #159_ — **Sessions summaries searchable — close the middle-tier recall blind spot (D-119).** → [archive](tasks-archive.md)
- [x] 128. _shipped 2026-06-11, PR #160_ — **Docs-completeness validator — turn tonight's audit into a build guarantee (the user 2026-06-10: "how do I…** → [archive](tasks-archive.md)

- [x] 129. _shipped 2026-06-13, PR #177_ — **`cmk config get/set/--show-origin` — settings without hand-editing JSON (v0.3.x; D-121).** → [archive](tasks-archive.md)
- [x] 130. _shipped 2026-07-18, PR #295_ — **`cmk purge --hard` — the true-deletion path. ABSORBED INTO TASK 96 (2026-06-28, D-218; was "ships WITH…** → [archive](tasks-archive.md)
- [x] 131. _shipped 2026-06-11, PR #161_ — **Remove the `cmk view` stub; park the viewer IDEA as a v0.4 product question (the user's call 2026-06-11:…** → [archive](tasks-archive.md)

- [x] 132. _shipped 2026-06-11, PR #162_ — **Fix the auto-extract dedup self-poisoning (CUT BLOCKER, found by cut-gate8).** → [archive](tasks-archive.md)

- [x] 133. _shipped 2026-06-11, PR —_ — **Allow-list `Skill(memory-search)` in `cmk install` (the Task-90 class repeating; cut-gate9 pre-session find).** → [archive](tasks-archive.md)

- [x] 134. _shipped 2026-06-11, PR —_ — **Extend the Poison_Guard secret catalog — fixed-prefix providers only (the user's 2026-06-11 directive: "as…** → [archive](tasks-archive.md)
- [x] 135. _shipped 2026-07-02, PR —_ — **Pack-completeness validator — the tarball must contain the full `template/` tree (the cut-gate9…** → [archive](tasks-archive.md)

- [x] 136. _shipped 2026-06-11, PR #164_ — **Clipped rich facts: drop, never write (cut-gate9 Session-2 find).** → [archive](tasks-archive.md)

- [ ] 140. **[D-253a trigger: ride-along the next time canonicalize.mjs is touched for ANY reason (byte-identical constraint stands); OR a Sonar re-flag]** **canonicalize: loop-based trailing-strip (the one REAL finding of the 31-hotspot Sonar triage, D-128).** `RE_TRAILING_PUNCT_WS = /[\s.,:;]+$/` is the textbook super-linear pattern (the StackOverflow-outage class). Unexploitable here (bounded local input — reviewed Safe with that justification), but the fix is 5 lines and free: replace with a character-scan loop. **HARD CONSTRAINT: byte-identical output** — canonicalize feeds the content-addressed fact IDs; a behavior change would orphan every existing ID. Test: the canonicalize vector fixtures must pass unchanged + a worst-case-input perf sanity.
  - Estimate: XS · Depends: none · _v0.3.x._
- [ ] 141. **[RE-VERDICTED 2026-07-10 (D-311, the trigger-fired walk): phase (a) was ALREADY SHIPPED in v0.3.x (PR #169, HC-8 + install-time ask + README note — D-260 caught the stale re-lane at the v0.4.4 build); phase (b)'s "or the v0.5.0 cut" trigger clause FIRED at the 2026-07-10 tag — verdict: phase (b) stays REJECTED-deferred (the D-162 perf rejection stands, ~10% slower FTS5; HC-8 + the install-time ask remain the npm-12 answer), trigger REFRESHED to: npm-12 GA + the FIRST real native-binding failure report (the fired cut clause dropped). Original disposition preserved below per the decision-trail rule.]** _Original (pre-2026-07-10): [D-253a — TIME-SENSITIVE, re-laned: phase (a) mitigation (doctor HC + README note) rides v0.4.4 (npm 12 ships ~NOW, July 2026); phase (b) node:sqlite migration trigger: npm 12 GA + the first native-binding failure report, or the v0.5.0 cut — whichever first]_ **npm v12 readiness (ships ~July 2026): the install-scripts default flips OFF — our native deps break on fresh installs (D-129).** `better-sqlite3` installs its binding via an install script (`prebuild-install` — already deprecation-warned), and `--with-semantic`'s `onnxruntime-node` does the same: under npm 12 a fresh `npm install -g` silently skips them → search/index crashes at first use. Two phases: **(a) v0.3.x mitigation** — reproduce under npm 11.16 warnings, add a `cmk doctor` HC ("native binding missing — npm 12 blocked the script; run `npm approve-scripts ...`"), README install note, `--with-semantic` runner guidance. **(b) the structural fix** — replace `better-sqlite3` with **`node:sqlite`** (built into Node 22.5+/24: zero native deps, zero install scripts, immune to the class; also ends the reinstall EPERM locked-DLL annoyance). `node:sqlite` supports `loadExtension` for sqlite-vec. The user (2026-06-12): "we can upgrade our node version to latest at any time" — the engines floor is NOT a blocker; spike the sqlite-vec extension load + the better-sqlite3 API-surface migration (index-db/search/semantic-backend callers). No git/URL deps in the kit, so `--allow-git`/`--allow-remote` are no-impact.
  - [x] 141a. **Phase (a) mitigation** — _shipped 2026-06-12 (D-134)._ **UX pivot at task start (the user's steer: "when you install you ask the user, not a secondary command after"):** the original plan made the doctor HC the discovery point; the shipped design makes **`cmk install` probe the binding up front and ask to fix INLINE** (`[Y/n]` → runs the remedy itself); the new **HC-8** is the backstop only. Primary-source corrections to the original spec wording (GitHub changelog 2026-06-09 + npm config docs): the block covers IMPLICIT node-gyp builds (better-sqlite3's exact shape), and `npm approve-scripts` is project-only — **global installs need the `allow-scripts` config** (`npm install -g <pkg> --allow-scripts=<dep>`), which is what the kit emits. `--with-semantic`'s runner passes `--allow-scripts=onnxruntime-node` itself on npm ≥ 11.16 (version-probed, no unknown-config noise on older npm). New module: `native-binding.mjs` (probes distinguish not-installed from installed-but-binding-broken — the semantic loader collapses both).
  - [x] 141b. **Phase (b) `node:sqlite` migration — REJECTED on perf 2026-06-16 (D-162); better-sqlite3 stays.** The three D-147 gates: Spike 1 (node:sqlite ships FTS5 on Node 24) ✅, Spike 2 (sqlite-vec loadExtension) ✅, Spike 3 (perf bake-off) ❌ — CLEAN CI data (`bench-storage.yml`, run 27594513495, noise ±0.3–3.7%) showed **node:sqlite ~10% SLOWER on FTS5 keyword search (1.105×, ±0.8%)** + ~32% slower on incremental reindex; only sqlite-vec KNN tied. Per D-147's no-measurable-regression bar, the migration loses (search latency is paid every query; the npm-12 prompt is one-time). 141a's install-time ask stands as the npm-12 answer. The bench harness + CI workflow are kept (re-run if a future Node release fixes node:sqlite's FTS5 perf). _Was specced below; unblocked, M-sized, slotted v0.3.2 (D-146) — then rejected on the perf gate._ **GATED on TWO spikes that both must pass BEFORE migrating (D-147):** (1) **perf bake-off** — `scripts/bench-storage.mjs` times `node:sqlite` vs `better-sqlite3` on the kit's real hot paths (gated read/search: FTS5 keyword · sqlite-vec cosine · per-read incremental reindex; reported-not-gated: reindexFull · single `cmk remember` write), N≥20× warm, interleaved A/B, same corpus; **bar = NO MEASURABLE REGRESSION (D-147, the user's hardened call): `node:sqlite_p95 ≤ better_p95 × 1.03` per gated read path (3% = harness noise floor, not a budget)**; if it FAILS, 141b does NOT ship and 141a's install-time ask stays the npm-12 answer — *"slower is worse than asking the user y/n on script install… we dont sacrifice the kit's purpose"* (the user). (2) **cross-platform `sqlite-vec` loadExtension spike** (Win/mac/Linux). (3) **`node:sqlite` FTS5-module spike** (added 2026-06-15, Task 153 research): confirm `node:sqlite` ships FTS5 compiled in on all 3 platforms — `better-sqlite3` prebuilts include it by default, `node:sqlite` may NOT (real signal: openclaw #62328 "node:sqlite missing FTS5 module — memory search keyword fallback broken"); the kit's entire keyword layer is FTS5, so if it's absent 141b does NOT ship regardless of perf/vec. Cheapest of the three — run FIRST as the fast-fail gate. Also rides 141b: the Node-engines-floor bump (≥22.5).
  - Estimate: M (b) / S (a) · Depends: none · _v0.3.x; (a) before July — DONE._
- [x] 142. _shipped —, PR —_ — **`cmk import-claude-md` — onboard from the rules people already have (D-130).** → [archive](tasks-archive.md)
- [x] 143. _shipped 2026-06-13, PR —_ — **Semantic near-duplicate detection at write time (D-130).** → [archive](tasks-archive.md)
- [x] 144. _shipped 2026-06-12, PR —_ — **`cmk doctor` memory-HEALTH section — content quality, not just plumbing (D-130).** → [archive](tasks-archive.md)
- [x] 145. _shipped 2026-06-12, PR —_ — **Session-start status line — make the invisible visible (D-130).** → [archive](tasks-archive.md)
- [ ] 146. **→ PRIOR ART (D-374 sweep): reuse the harness in [`2026-07-13-sqlite-reader-freshness.md`](../docs/research/2026-07-13-sqlite-reader-freshness.md)** — it already built the WAL/isolation methodology and ran 3 empirical repros against the kit's own index db, which is exactly this task's open sub-question (b) (concurrent-writer stress). Do not re-derive it. **Cross-link:** this probe is also what tells whether **Task 238**'s parallel-agent trigger has fired — the two tasks were not cross-referenced. **→ SWEEP VERDICT 2026-07-18 (D-353): stale v0.3.x lane refreshed to a named trigger — a multi-agent/swarm consumer appears (a user asks, or the Task-127 companion is scoped; the U-Mem seed class).** **Kit x Claude Code WORKFLOWS composition: concurrent MCP access + swarm recall (D-131).** Dynamic workflows spawn up to 16 CONCURRENT subagents (1,000/run), each starting COLD in the project - the kit's amnesia problem times a hundred, and the kit already reaches them through two existing doors: the CLAUDE.md managed block (loads per agent) and the inherited tool allowlist (`mcp__cmk__*` since Task 108 - every swarm agent can `mk_search` settled decisions prompt-free). UNTESTED composition: each agent spawns its OWN stdio `cmk mcp serve` against the SAME SQLite index - concurrent reads should hold (WAL), but N agents calling `mk_remember` simultaneously is lock-contention territory never exercised (the lock-discipline + better-sqlite3 busy-timeout surface; composes with 141b's node:sqlite migration). Investigate: (a) a live workflow whose agents query mk_search (does recall reach a swarm?); (b) a concurrent-writes stress (N parallel mcp serve processes writing); (c) document the kit-x-workflows story in README/docs if it holds ("your swarm shares one memory"); (d) design question from the squad review (2026-06-12): per-ROLE memory partitions for swarm agents (squad gives each agent a charter.md + history.md alongside shared decisions) - shared facts + per-role histories may beat one undifferentiated pool at swarm scale.
  - **🔬 PRIMARY-SOURCE GEM + STATUS (deep-read 2026-06-28; D-218).** Confirmed a REAL data-loss path, not just theoretical: `appendScratchpadBullet` uses lockless `readFileSync`/`writeFileSync` on the markdown, and the SQLite index has **zero `busy_timeout`** → immediate `SQLITE_BUSY` under N concurrent servers. **mem0** names our exact race (funnel concurrent read-modify-write into ONE batched pass). **letta** ([`orm`](https://github.com/letta-ai/letta)) uses an optimistic `version_id_col` → raises `StaleDataError` so a lost write is LOUD, never silent — the pattern to steal. **Fit nuance:** the DB half needs only a `busy_timeout` (cheap); the MARKDOWN half needs the lock (reuse auto-extract's existing `O_CREAT|O_EXCL` lock-discipline). Patch-shaped. _VERDICT: keep-as-own-task; backlog-no-version-yet, RIDES Task 50 — the cross-agent work multiplies the concurrent servers, so live-test the race THERE (today, with one agent, it's mostly latent). Sequence alongside whichever agent release stresses it._
  - **🔬 REVISIT-WHEN-BUILDING — `songth1ef/nestwork`'s PARTITION-BY-WRITER concurrency (deep-read 2026-06-29; D-227).** A git-native markdown multi-agent memory tool (11★, license-unspecified — patterns not code) solves the concurrent-write race CHEAPER than locks/CRDT/busy-timeout: **each agent OWNS its own directory** (`agents/<host>/<agent-id>/memory.md`) so write paths NEVER overlap → no conflict resolution needed; plus a PreToolUse `git pull --rebase` before write + PostToolUse commit+push after, collapsing the race window to a single op. **For us:** if Task 50 ever has multiple agents writing the SAME shared `context/` concurrently, the cheapest fix may be PARTITION-BY-WRITER (per-agent scratchpad files) rather than locking the shared file — sidesteps the lock entirely for the per-agent surface, keeping locks only for the genuinely-shared facts. A real alternative to the lock-the-shared-file approach (mem0/letta) noted above. _Relates Task 50 (cross-agent — where the partition would live), the lock-discipline; D-227._
  - Estimate: S/M · Depends: none (141b strengthens) · _v0.3.x._
- [x] 147. _closed 2026-07-18, PR —_ — **`cmk digest` (regenerated render) + `context/DECISIONS.md` (APPEND-ONLY permanent journal) (D-132,…** → [archive](tasks-archive.md)
- [x] 148. _shipped 2026-07-08, PR #264_ — **Auto-judged privacy — sensitivity is Claude's judgment, not the user's tag (D-150; ADR-0019; design §6.10;…** → [archive](tasks-archive.md)

- [x] 150. _shipped 2026-07-02, PR —_ — **The automatic-path criterion holds: the proposal appears with NO manual command.** → [archive](tasks-archive.md)

- [x] 151. _complete 2026-06-30, PR —_ — **Persona-promotion model — reconsider the human-gated review-queue (BROADER DESIGN; v0.4 curation lane;…** → [archive](tasks-archive.md)

- [x] 153. _shipped 2026-06-15, PR —_ — **FTS5 query sanitization — dots and version strings crash `mk_search` (surfaced 2026-06-15).** → [archive](tasks-archive.md)

- [x] 154. _shipped —, PR —_ — **`.gitattributes` LF-pinning for `context/` — ALREADY SHIPPED in v0.3.1 (D-126/D-145); filed-then-retired…** → [archive](tasks-archive.md)

- [x] 155. _shipped —, PR —_ — **`cmk get --include-tombstoned` — explicit, opt-in HUMAN-only tombstone recovery (D-163).** → [archive](tasks-archive.md)

- [x] 156. _shipped —, PR —_ — **DECISIONS.md recall design + impl — the AI recalls decision HISTORY from the journal (D-168, design §19.4.2).** → [archive](tasks-archive.md)

- [x] 157. _shipped 2026-06-16, PR —_ — **Bug 1 — `cmk reindex --full` UNIQUE-constraint crash (id-keyed vs file-scoped delete).** → [archive](tasks-archive.md)

- [x] 158. _shipped 2026-06-16, PR —_ — **Bug 2 — stale version in the SessionStart snapshot → DEFERRED to the Task 66 → 95 chain (design-resolved…** → [archive](tasks-archive.md)

- [x] 159. _shipped 2026-06-18, PR —_ — **AUTO-SYNC the decision journal — `DECISIONS.md` stays current with NO command (completes Task 147's "made…** → [archive](tasks-archive.md)

- [x] 160. _shipped 2026-06-18, PR —_ — **CLAUDE.md diet — migrate heavy PROCEDURAL disciplines out of the always-loaded rules file into on-demand…** → [archive](tasks-archive.md)

- [x] 161. _shipped 2026-06-19, PR #206_ — **Session-buffer compression times out as `now.md` grows (v0.3.4, D-171).** → [archive](tasks-archive.md)

- [x] 162. _shipped 2026-06-19, PR —_ — **No user-facing UPDATE path (v0.3.4, D-172).** → [archive](tasks-archive.md)

- [x] 164. _shipped —, PR —_ — **Lint-clean memory output — the kit's committed `context/` passes a default markdown linter by emitting…** → [archive](tasks-archive.md)

- [ ] 166. **→ SWEEP VERDICT 2026-07-18 (D-353): stale v0.4.x/v0.5 lane refreshed to a named trigger — kiro-cli V3 hook support reaches GA, or a Kiro user reports the guardrail gap live.** **kiro-cli V3 (2.9+) hook support — the delete-guardrail + any new triggers via the V3 mechanism (D-198 follow-up).** kiro-cli V3 redesigned hooks as a BREAKING CHANGE (proven live 2026-06-24, KIRO_VERSION=2.9.0): embedded agent-config hooks moved to **standalone `.kiro/hooks/*.json` files with PascalCase triggers**, and tool-blocking moved from `preToolUse` to a new **`permissions.yaml`** capability model (kiro.dev/docs/cli/v3). Our V2-style embedded `agentSpawn`/`stop` STILL fire on 2.9.0 (capture/inject work — D-198 proven), but V2 `preToolUse` does NOT — so the **delete-guardrail doesn't fire on kiro-cli V3** (it falls back to kiro-cli's own shell-approval prompt). 2.9.0 also renamed the shell tool `execute_bash`→`execute_command` (already added to `guard-memory.mjs` SHELL_TOOLS). **Done when:** the kit emits the V3 guardrail (research the `.kiro/hooks/*.json` PascalCase schema + `permissions.yaml`; decide V3-only vs both-V2+V3 by detecting the installed kiro-cli version), live-verified blocking a memory delete on a real 2.9+ session; capture/inject confirmed still working on both V2 + V3. · Estimate: M (research + a new emit path + version-detection) · Depends: none · _v0.4.x or v0.5 — NOT a v0.4.0 blocker (capture/inject, the core value, work on V3; the guardrail has Kiro's native fallback). Relates D-198._

- [x] 167. _shipped 2026-06-25, PR —_ — **Stale-injection bug — the lazy now.md→today roll (Task 105) doesn't recover a SEVERELY-bloated `now.md`,…** → [archive](tasks-archive.md)
- [x] 168. _shipped 2026-06-26, PR #237_ — **Three pre-existing test failures on the Windows dev checkout (surfaced during the Task 167 autopilot,…** → [archive](tasks-archive.md)

- [x] 169. _shipped 2026-07-02, PR —_ — **The `Skill(<name>)` allow-list stopped suppressing the prompt on Claude Code 2.1.x — add the…** → [archive](tasks-archive.md)

- [x] 170. _shipped 2026-07-02, PR —_ — **`cmk install --with-semantic` trusted npm's EXIT CODE instead of verifying the embedder actually imports —…** → [archive](tasks-archive.md)

- [x] 173. _shipped 2026-06-28, PR —_ — **Fix the 3 open CodeQL code-scanning alerts — a security patch (v0.4.2, 2026-06-28).** → [archive](tasks-archive.md)

- [x] 172. _shipped 2026-06-27, PR —_ — **PROMPT-FREE for real: a `PermissionRequest` hook auto-approves the kit's own tools/skills — the working…** → [archive](tasks-archive.md)

- [x] 171. _verified 2026-07-02, PR —_ — **The `mcp__cmk__*` server WILDCARD stopped auto-approving the kit's MCP tools on Claude Code 2.1.x —…** → [archive](tasks-archive.md)

- [x] 174. _shipped 2026-07-20, PR —_ — **Git-history backfill — reconstruct a working-day's session log when commits exist but no `sessions/` entry…** → [archive](tasks-archive.md)

- [x] 175. _shipped 2026-07-20, PR #309_ — **`cmk tour` — narrate the user's OWN `context/` memory (onboarding/explainer; idea borrowed from…** → [archive](tasks-archive.md)

- [x] 176. _shipped 2026-07-19, PR #307_ — **Graph-recall research + ADR — should the kit add a relational/graph query mode alongside keyword +…** → [archive](tasks-archive.md)

- [ ] 177. **→ NO CLONE STEP NEEDED (D-374 sweep): `pro-workflow/src/optimizer/` is ALREADY cloned** at `C:\Projectsesearch-clones\pro-workflow\src\optimizer\` (aggregate/apply/reflect/trainer/validate .ts all present), and two 2026-07-19 notes already read that repo's search layer. Start at "read", not "clone". **Correction-trained behavior-doc research + ADR — can accumulated corrections TRAIN a durable instruction/behavior doc (not just store facts)? (research-then-decide; surfaced surveying `rohitg00/pro-workflow`, 2026-06-28; D-217.)** **The idea:** `pro-workflow` (a direct-competitor CC memory kit, npm, FTS5+SQLite) has a buried gem its README never mentions — a **DSPy-style offline skill optimizer** (`src/optimizer/`): it treats `SKILL.md` as trainable text-space state, uses past corrections as **training trajectories**, holds out 20% as a **validation set**, and runs Reflect→Aggregate→Clip→Apply→Evaluate (LLM-graded against the holdout, accept only if the score improves, budget-capped, periodic "slow" consolidation rewrite). The OUTPUT is an auto-improved instruction document. **Why it's interesting for US:** the kit currently remembers *facts* (and promotes durable ones to the persona — Task 151); it does NOT *train the behavior doc* from corrections. This is a sharper, mechanized version of what **Task 55** (behavioral pattern detection — "learn how I work") and **Task 151** (recurrence-scored promotion) gesture at: a correction isn't just a fact to store, it's a signal that could UPDATE a durable rule, **validated against held-out corrections** so the update is measured, not vibed. **Done when (research + decision, NO production code until decided):** ☐ characterize precisely what a "trainable doc" would be in OUR model — the persona scratchpads? a CLAUDE-rules-style behavior file? the skill docs? (be concrete: what doc, whose corrections, what's the validation signal); ☐ read the real `pro-workflow/src/optimizer/` loop + the DSPy/ReflACT prior art it's based on (CLONE + read, the Task 149 discipline); ☐ assess FIT against our markdown-as-truth + post-hoc-reversible (`cmk forget`) model — does an LLM-graded accept/reject gate cohere with the kit's "no human-in-the-loop ritual" (D-169) and "auto-judge, post-hoc revert" (D-154) postures, or does the validation-set gate re-introduce a heavyweight offline pass we don't want?; ☐ the hard honesty check — is this REAL leverage or a sophisticated solution to a problem semantic recall + recurrence-promotion already cover? (the adoption-evidence rule: concrete counterfactual, not "it felt powerful"); ☐ output an ADR: ADOPT (with the trainable-doc design + a version lane + how it composes with 55/151) / DEFER (with the revive trigger) / REJECT (with why fact-storage + recurrence-promotion suffices). **Compose with:** Task 55 (behavioral pattern detection — the closest sibling; this might BE its mechanism), Task 151 (recurrence-scored promotion — a correction-trained rule is a promotion target), Task 97 (dynamic trust — the validation score is a trust signal), D-169 (no manual ritual — the gate on whether an offline training pass fits), D-154 (auto-judge + post-hoc revert — the posture the gate must cohere with), the adoption-evidence discipline. · Estimate: M (research + ADR; an ADOPT verdict spawns a separate build task) · Depends: none (research-only; reads Task 55/151's design + the pro-workflow optimizer) · _v0.4 curation lane (research input, pairs with Task 55/151) **→ re-labeled 2026-07-10 (D-311): the curation RESEARCH lane is version-agnostic (slots per D-157); v0.4 was its birth-lane, not a deadline — trigger unchanged (runs when 55/151-adjacent curation work is next taken up)**. Provenance: surveying `pro-workflow` — its skill-optimizer is the one mechanism we genuinely lack (we're ahead on the core memory loop: it LOGS learnings to stderr, never injects them into context — the README's "auto-loaded" is terminal-only; ours injects the frozen snapshot). The optimizer trains the BEHAVIOR doc, a bet we don't make. The user: "add research task." D-217. Relates Task 55 (behavioral patterns), Task 151 (promotion), Task 97 (dynamic trust), the read-past-the-README discipline, the adoption-evidence rule._
  - **🔑 KEY UNBLOCK — the "outcome signal WITHOUT a user ritual" gap (the open hard part of this task AND Task 97) has a working answer: memclaw's 6 PASSIVE signals (deep-read 2026-06-29; D-223).** Both 177 (train-the-doc) and 97 (dynamic trust) kept hitting the same wall: an LLM-graded / outcome-scored update needs a SUCCESS/FAILURE signal, but asking the user for it violates D-169 (no ritual) — and pro-workflow's DSPy optimizer + caura's `report_outcome` both ASSUME an explicit signal we don't want. **caura-memclaw's Forge cracks it: infer outcome PASSIVELY from signals the kit ALREADY produces** (`session_trace.py`): (1) **contradiction-detected** soon after a recall → that memory was wrong; (2) **fact superseded** (`superseded_by`) → the old one failed; (3) **repeat-recall** of the same query → the first answer didn't land; (4) **terminal-memory classification** — an LLM labels the last memory of a session "shipped/fixed" vs "blocked/abandoned"; (5) **cross-session reuse** — a fact recalled across many sessions with healthy outcomes is load-bearing; (6) optional **git-commit/CI** tied to the session. **We already PRODUCE signals 1, 2, and 5** (contradiction-queue, supersede links, the index's access tracking) — so a passive outcome estimate is reachable WITHOUT a prompt. This is the D-169-compatible "did the correction/rule actually help?" measure that makes a validated training loop (177) or an evolving-trust score (97) buildable instead of stuck. **Revisit at build time** — design the passive-signal estimator as the validation signal for 177's accept/reject gate AND 97's confidence delta; it's the same primitive both need. _Relates Task 97 (the SAME unblock — passive signal = the confidence delta), Task 180 (Forge, where this lives), D-223, D-169 (why passive-not-prompted matters)._

- [x] 178. _shipped 2026-07-19, PR #306_ — **Hybrid-search tuning research — evaluate Reciprocal Rank Fusion (+ other fusion/ranking methods) against…** → [archive](tasks-archive.md)

- [x] 179. _closed 2026-07-18, PR —_ — **Memory-improvement loop — the umbrella research/ADR: should the kit have ONE recurring background loop…** → [archive](tasks-archive.md)

- [ ] 180. **Memory → skill SYNTHESIS — generate a bespoke skill FROM the user's accumulated memory/persona, + a possible improve-loop (EXPLORATORY — research whether it's even worth it; the user's idea 2026-06-28, flagged "maybe going too far"; D-219).** **The idea (the user, verbatim):** *"we can create a skill based on how the user works and his instructions and memories, and create a skill based on it, and then a loop that improves that skill… I don't know if we are ready for this, maybe I'm going too far… maybe this is a different task at all."* So: take the captured persona (HABITS / working-style / LESSONS) + project conventions + decisions, and SYNTHESIZE a custom Claude Code skill (e.g. a generated `the-way-I-build-backends` skill) that encodes the user's way of working — then optionally a loop that refines that generated skill as memory grows. **Why it's DISTINCT from everything else (so it's not muddled again):** the kit's persona work (Task 151) INJECTS facts into context (a snapshot); this would EMIT an ARTIFACT — a `.claude/skills/<name>/SKILL.md` the agent loads on-demand. Different output, different surface. It is NOT Task 179's loop (that improves MEMORY + persona-facts), NOT Task 177 (that trains an existing behavior doc), NOT Hermes skill-curation (we have no skill library to prune). It's "memory as the INPUT to author a new skill." **⚠️ EXPLORATORY — the honest flags (this task's FIRST job is to decide if it's worth doing AT ALL):** ☐ is there a real user need, or is the injected persona already sufficient (a generated skill may just duplicate what the snapshot already carries)? ☐ does a generated skill compose with Claude Code's skill model (auto-invoke `description`, `allowed-tools`) without becoming noise/another thing to maintain? ☐ does it violate D-169 (a skill the user must manage) or the kit's "memory just works, you don't curate it" thesis? ☐ the improve-loop adds the pro-workflow/DSPy validation question (Task 177's territory) — does it collapse INTO 177, or is generating-from-scratch genuinely different from training-an-existing-doc? ☐ prior art to read at task-time: pro-workflow's optimizer (trainable SKILL.md), the gstack/gskill skill-layer notes (2026-05-30), Anthropic's skills doc, any project that GENERATES skills from usage. **Done when:** a short research note + a GO/NO-GO call (is this worth a build task, or does the persona/177 already cover the need?) — NOT a commitment to build. If GO, it spawns a real design task with a lane; if NO-GO, document why (likely "the injected persona already delivers this; a generated skill is duplicate maintenance"). · Estimate: S (a scoping research note + go/no-go — deliberately small; this is "is the idea real?" not "build it") · Depends: none (reads the persona/151 design + the skills doc) · _v0.4 curation lane (exploratory research; sibling-but-separate to 179) **→ re-labeled 2026-07-10 (D-311): version-agnostic research lane (slots per D-157); trigger unchanged (the GO/NO-GO scoping note runs alongside the next curation-lane pickup)**. Provenance: the user's "create a skill from how the user works… then a loop that improves it" + the self-aware "maybe I'm going too far, maybe a different task" (correct — it IS a different task, filed separately so it's not lost AND not forced into 179). D-219. Relates Task 151 (persona injection — the closest existing surface), Task 177 (train-the-doc — may absorb the improve-loop half), Task 179 (the memory-loop — explicitly NOT this), the gstack skill-layer notes, D-169 (the no-manual-management gate this must pass)._
  - **🔬 PRIMARY-SOURCE REFERENCE — caura-memclaw's "Forge / Skill Factory" is a WORKING memory→skill-synthesis pipeline (deep-read 2026-06-29; D-223). UPGRADES this task from "maybe too far / no prior art" to "exploratory but with a proven reference design."** memclaw (Apache-2.0) BUILT exactly this (it was vaporware at our 2026-06-12 README review; now production code: `core_api/services/forge/forge_service.py` + `distill_prompt.py` + `fingerprint.py` + a spec/impl-plan). The real pipeline, adapted to OUR single-user + markdown model: **(1) build session traces** + infer outcome from **6 PASSIVE signals — NO user ritual** (contradiction-detected, fact-superseded, repeat-recall=first-answer-failed, terminal-memory-classification "shipped/fixed" vs "blocked/abandoned", cross-agent reuse, optional git-commit/CI hooks tied to session) — this is the D-169-compatible answer to "where's the success signal?"; **(2) cluster** repeated workflows (memclaw uses entity-Jaccard ≥40%; for us = recurrence across SESSIONS/PROJECTS, since we're single-user not a fleet); **(3) gate on volume + diversity** (≥N recurrences before synthesizing — don't make a skill from one-off noise; their "≥3 distinct agents" → our "≥N distinct sessions/projects"); **(4) distill via LLM** (`distill_prompt.py` is a ready template) into a candidate `SKILL.md` with PROVENANCE (`cites:[mem-ids]`, `evidence:"across 4 sessions…"`, `cluster_fingerprint`); **(5) fingerprint** (`fp:v1:sha256(goal+domain+top-entities+step-skeleton)`) + a **rejected-fingerprints cooloff table** (don't re-propose a skill the user already rejected — for us a local `.json` + 30d TTL); **(6) NEVER auto-promote** — candidates stay `candidate` until evidence gates pass (recurrence + convergence + freshness) = EARN-IT, not auto-apply (composes with 151's recurrence-scoring + D-154 post-hoc-revert). **The honest catch:** memclaw is Postgres + multi-tenant + FLEET (its diversity gate is "across agents" — meaningless for one user); we adapt the SHAPE (passive-signal outcomes, fingerprint-dedup, earn-it gates, recurrence-across-sessions), NOT the server machinery. **This makes 180's GO/NO-GO sharper:** the question is no longer "is memory→skill even possible?" (memclaw proved it) but "does a single user generate enough recurrence to clear the earn-it gate, or does the injected persona already cover it?" _Relates Task 97/177 (the passive-outcome-signal gem cracks their open "signal without ritual" gap too), Task 95 (the fingerprint-dedup-rejected pattern), Task 151 (earn-it = recurrence-scoring), D-223._

- [x] 181. _shipped 2026-07-01, PR —_ — **Recurrence measures FRICTION, not adherence — a silently-followed rule never promotes…** → [archive](tasks-archive.md)

- [x] 182. _shipped 2026-07-01, PR —_ — **Two-pass review (self + code-review-excellence): 0 blocking, 1 important (I1) decided + tested.** → [archive](tasks-archive.md)

- [x] 183. _shipped 2026-07-01, PR —_ — **The scaffold's `(example)` placeholder bullets are indexed + returned by `cmk search`, polluting results —…** → [archive](tasks-archive.md)

- [ ] 184. **→ PRIOR ART (D-374 sweep, 2026-07-20): READ [`2026-05-22-primary-source-examination.md`](../docs/research/2026-05-22-primary-source-examination.md) §4/5 BEFORE designing the registry-vs-central-index fork.** basic-memory ships `project_management` + `workspaces` MCP tools and that note says verbatim *"they've already solved the multi-project navigation problem"* — written 2026-05-22, never picked up when this task was drafted 2026-07-01. Already cloned: `research-clones/basic-memory/src/basic_memory/api/v2/routers/project_router.py`. **Cross-project search — `cmk search --all-projects` (promoted from a v0.1.0 deferred one-liner to a real trackable task; the v0.4.3 cold-open made the case, 2026-07-01; D-247).** **The deferral (verbatim, tasks.md v0.1.0 deferred-ideas list):** *"Cross-project search (`cmk search --all-projects`) — v0.2."* It has sat as an un-scoped bullet since v0.1.0 — now a real entry because the Session-3 cold-open showed WHY it matters. **The gap:** `cmk search` is scoped to ONE project's index (`<projectRoot>/context/.index/memory.db`). A query from project B cannot reach a fact captured in project A — even though the user often WANTS that ("how did I solve this in my other project?", "what's my standard X across everything?"). Today the ONLY cross-project channel is (1) the user-tier persona (a curated SUBSET, promoted via `cmk lessons promote`) and (2) injection of that persona at SessionStart. There is no way to SEARCH across all your projects' captured facts. **Why the cold-open makes the case:** in Session 3, the fresh project couldn't SEARCH the user's uv/ruff preferences (they lived in cut-gate19's project tier + the un-indexed persona — Task 182); a true `--all-projects` search would let a new project find a relevant fact from ANY prior project, not just the hand-promoted persona slice. **Design questions to resolve at task-time (this is DESIGN-first):** ☐ how are project index-dbs discovered/registered (a user-tier registry of known project roots? a scan? explicit `cmk project register`)? ☐ merge-search across N sqlite dbs vs. a central user-tier index that mirrors all projects (the latter re-raises the privacy + staleness questions)? ☐ how does it compose with the deferred `cmk persona sync <git-url>` (design §16, 72.2) — is cross-project search a LOCAL-only feature or does it want the synced remote? ☐ ranking across projects (recency? per-project trust? project-relevance?). ☐ privacy: a cross-project result surfaces project A's content in project B's session — is that always wanted, or opt-in per query? **Done when:** a design note (registry vs central-index, the privacy model, the ranking) + a GO/NO-GO; if GO it spawns the build task. NOT a commitment to build in a specific version yet. · Estimate: M (design) + M–L (build, if GO) · Depends: none (but composes with 182 persona-search + the 72.2 persona-sync deferral) · _Relates Task 182 (in-project persona-unsearchable — a PREREQUISITE-adjacent fix; both are "search can't see what the user expects"), Task 65/46 (the search engine this extends), design §16 / 72.2 (`cmk persona sync` — the cross-machine sibling). Provenance: the v0.1.0 deferred-ideas bullet, promoted after the v0.4.3 cold-open surfaced the cross-project recall gap. D-247. **→ RE-VERDICTED at the v0.5.0-cut walk (the D-307-era sweep): untouched, KEEP with the same trigger (persona-sync design not taken up, no repeated user asks); next re-verdict point = the v0.6 cut (annotated 2026-07-10, D-311).**_

- [x] 185. _shipped 2026-07-02, PR —_ — **The BACKLOG-TRIAGE session — one dedicated pass over EVERY open task: still-relevant? / lane-to-a-version?…** → [archive](tasks-archive.md)

- [x] 186. _shipped 2026-07-20, PR #310_ — **Consolidate the 4 doc-validators into ONE manifest-driven `validate-docs.mjs` (structural half of the…** → [archive](tasks-archive.md)

- [x] 187. _shipped 2026-07-02, PR #247_ — **SonarCloud analyzer crashes on `tests/cli-forget.test.js` → flaky-red quality gate (upstream SonarJS bug;…** → [archive](tasks-archive.md)

- [x] 188. _closed 2026-07-18, PR —_ — **Escalate-to-the-user, then CAPTURE the resolution — the "user as last-resort teacher" affordance (NEW…** → [archive](tasks-archive.md)

- [ ] 189. **→ FIRED-WALK VERDICT 2026-07-20 (D-372): its trigger ("triage at the Task 185 sweep") FIRED — 185 shipped — so the triage is due and this is it. LANED v0.7.0 as a RIDER on Task 95**, on the Task-212 precedent (that task was laned as 194's before/after instrumentation): 95 re-curates the corpus, and 189 is exactly the measurement that says whether the re-curation HELPED. Measuring ROI before and after a curation pass is worth far more than measuring it against an un-curated baseline alone, and 95's slot is now committed (D-372), so the dependency is real rather than speculative. **Recurrence = memory ROI — MEASURE how much the kit's memory is actually helping, per project (NEW out-of-the-box idea from the U-Mem harvest; potential differentiator; 2026-07-01; D-251).** **The finding (U-Mem's strongest empirical result):** Pearson **r = 0.888** between task-similarity (how much a new task resembles past ones) and the performance gain memory provides — *"memory is a bet on repetition."* Memory helps most in high-recurrence domains (same codebase / conventions / problem-shape), near-zero for wildly-different one-off work. **The out-of-the-box idea:** no surveyed memory product MEASURES its own recurrence/ROI. The kit could — using the semantic backend it already ships (Task 65, sqlite-vec + embeddings) — compute a per-project "recurrence score" (how similar is this session's work to the captured history?) and (a) TELL the user how much memory is helping them ("your work is highly recurring — memory is paying off" vs "mostly novel this session — memory has less to offer"), and/or (b) ROUTE capture effort toward high-recurrence domains where it pays. This turns U-Mem's *analysis metric* into a *user-facing product feature* — a "your memory is working" signal that's honest (it can say "not much this session") and unique. **⚠️ RESEARCH-then-decide (do NOT commit to build — this needs its own design):** ☐ is a recurrence score ACTIONABLE for a single user, or just a vanity metric? (the honest risk: knowing "memory helped 12%" may not change anything the user does); ☐ can it be computed cheaply from the existing embedding index (AMCS-style average-max-cosine-similarity between new facts and the corpus) without a new pipeline? ☐ does it surface in `cmk doctor` / a `cmk stats` view, or the snapshot? ☐ does routing capture-by-recurrence conflict with "capture everything, it's cheap" (the kit's current stance)? ☐ is this a differentiator worth a lane, or a research-note curiosity? **Done when:** a research note (is the metric actionable + cheap-to-compute on our stack?) + a GO/NO-GO; if GO it spawns a design task with a version lane. NOT a build commitment. · Estimate: S (scoping research note + go/no-go; a build, if GO, is M) · Depends: Task 65 (the semantic/embedding layer any recurrence score reuses) · _v0.5+ candidate (a "measure-your-own-value" feature, past the core loop). **Trigger (D-248): triage at the Task 185 sweep; if GO, lane after Task 65's semantic layer is a stable base to build the AMCS metric on. → TRIAGED at the v0.5.0-cut walk (recorded in the D-307-era sweep entry): the recall-log dependency shipped (Task 190), verdict KEEP as a natural v0.5.x follow-on; annotated here 2026-07-10 (D-311) so the entry stops pointing at a passed sweep.** Provenance: the U-Mem harvest (r=0.888 recurrence↔ROI) + the user's "give us ideas out of the box." Relates Task 65 (the embedding index it reuses), U-Mem §4.4 (the AMCS metric + Fig 4), D-251. **➕ Sibling filed (2026-07-10 sweep, D-308): Task 212** — AutoMem-style PROCESS-health metrics (writes-per-search / empty-search / redundant-write / snapshot cost) from the logs the kit already writes; 212 = "is the memory process healthy" (no embeddings, can ship first), 189 stays "is memory helping the USER" (the ROI question, keeps its research gate)._

- [x] 190. _shipped 2026-07-06, PR —_ — **RECALL-LOG — record which memory IDs surfaced each turn (the learn-loop's attribution primitive; ADR-0017…** → [archive](tasks-archive.md)

- [x] 191. _shipped 2026-07-07, PR —_ — **Expectation pre-registration + `judgment_*.md` files — the earned-judgment wedge (ADR-0017 Phase 1b; D-252).** → [archive](tasks-archive.md)

- [x] 192. _shipped 2026-07-07, PR —_ — **The Stop-hook JUDGE — extend the passive-signal set to the full portfolio (ADR-0017 Phase 1c; D-252).** → [archive](tasks-archive.md)

- [x] 193. _shipped 2026-07-06, PR —_ — **FEEDBACK-SCREEN — Poison_Guard for the loop (ADR-0017 Phase 1d; a BUILD PREREQUISITE for 192/194; D-252).** → [archive](tasks-archive.md)

- [x] 194. _shipped 2026-07-13, PR #287_ — **Close the edge — confidence-gated SEARCH blend + survival gate + anti-pattern conversion (ADR-0017 Phase…** → [archive](tasks-archive.md)

- [x] 195. _shipped 2026-07-02, PR —_ — **The cross-agent product NAME decision — execute ADR-0012's deferred half (its trigger has FIRED; surfaced…** → [archive](tasks-archive.md)

- [ ] 196. **→ SWEEP VERDICT 2026-07-18 (D-353): core SHIPPED (Cursor v0.4.5, Codex v0.4.6) — the entry's remaining scope is the on-demand tail (Antigravity/gemini-cli/opencode), trigger: demand (a user asks for an agent).** **Cursor adapter — the next committed agent + the ordered harness tail (made sweep-visible; D-255).** **Cursor** was the locked v0.4.1 slot, POSTPONED 2026-06-25 (the user's call — Task 167's now.md fix took the slot); RELEASE-PLAN keeps it as "still a committed agent, just no longer the next slot" — but it carried NO task number and NO pickup trigger, so the D-253 sweep couldn't see it (the D-255 class). This task carries it + the tail: **Cursor → Codex → Google Antigravity → gemini-cli → opencode → cline/roo/windsurf/…** (order shifts on real signal, per D-157). **The build shape is settled** (Task 50's seam): each agent = a thin `defineAgentProfile` DATA declaration + its paths primary-source-verified (the §5.1 rule — never trust convergence) + live-tested; genuinely patch-sized; the parity validator enforces leg-completeness. Per D-157 the patch DIGIT is assigned when it ships — this task exists so the WORK is sweep-visible, not to pre-number it. **Done when (per agent, Cursor first):** ☑ profile as pure data on the 50.C factory (`cursor` in agent-profiles.mjs, `hooks-mcp`, the new data-driven `instructionFrontmatter` field); ☑ paths verified against the agent's OWN docs (cursor.com agent/hooks + context/mcp + context/rules, 2026-07-04 — `.cursor/hooks.json` `{version, hooks}`, `.mdc`-required rules, per-event payload/response schemas; the market/coexistence layer in the D-268 landscape note); ☑ `cmk install --ide cursor` wires end-to-end in one step (the GENERIC seam route, no bespoke orchestrator — `cmk cursor-hook` dispatcher, 6 events incl. the D-192 shell guard; doctor Cursor-aware; design §16.50.3); ☐ capture/inject live-verified in a real session — **CLI-side live-verified 2026-07-04** (fresh `c:\tmp` sandbox, real bins: install → sessionStart inject carried the on-disk fact → prompt+turn capture landed in `now.md` + the bi-turn transcript + spawned the real extract child → guard denied `rm -rf context/memory` via `{permission:"deny"}` → uninstall left no kit residue; **this live-test also caught D-269** — the Kiro+Cursor inject legs read a non-existent field and emitted EMPTY since v0.4.0, fixed + integration-locked); **the real-Cursor-session half is the user's manual gate step** (drive a real Cursor chat, watch the snapshot arrive + a fact capture); ☑ README/CHANGELOG + the parity validator green. · Estimate: S-M per agent (the seam did the hard part) · Depends: Task 50 (shipped) · _**TRIGGER FIRED + LANED 2026-07-02 (the user's Option A call, D-257): the demand signal exists — the maintainer's friends use Cursor AND Codex. The breadth promise is honored BEFORE v0.5: after v0.4.4 ships, the next two slots are Cursor, then Codex (expected v0.4.5 / v0.4.6 — digits still stamped on cut per D-157, but the ORDER is committed); the rest of the tail stays demand-driven; v0.5 (the learn-loop) opens after the two committed agents ship.** Relates Task 50 [x] (the seam), 50.N/165/166 (the Kiro leftovers, same surface), D-157 (assigned-on-cut), D-255, D-257._ · **CURSOR SHIPPED 2026-07-04, PR #254 (the v0.4.5 lane — digit stamped on cut per D-157).** Cursor rode the GENERIC per-profile seam untouched (no bespoke orchestrator — the D-180 proof-of-thesis); the `cmk cursor-hook` dispatcher + 6-event map + doctor + uninstall all landed; the live-test caught + fixed D-269 (the Kiro+Cursor inject legs read a non-existent field, EMPTY since v0.4.0). **The task stays OPEN — Codex + the ordered tail remain, and the live-Cursor-SESSION gate is the user's manual step.** Next agent: Codex (v0.4.6). **→ CODEX SHIPPED 2026-07-12 (the v0.5.2 lane; D-327):** the June-20 "plugin-marketplace" classification was OBSOLETE — Codex now has first-class hooks (Claude-compatible envelope), so it rode the GENERIC seam as pure data + two mechanism branches: `codex` profile (hooks-mcp; `.codex/hooks.json` matcher-group shape; AGENTS.md instruction) + `cmk codex-hook` dispatcher (Stop-capture reads the rollout via the payload's `transcript_path` — `readCodexTurn`, real-capture fixture) + MCP via `codex mcp add` (never TOML surgery; evidence-gated `mcp remove` on uninstall) + `CodexExecBackend` (`codex exec -s read-only --json`, live-verified 0.142.5) + doctor HC-1 codex branch + docs/CODEX.md. **Honesty line: Codex hash-trusts hooks — the user runs `/hooks` once or the hooks stay silent** (documented everywhere; the Kiro one-time-trust class). The interactive live-Codex-session gate = the user's manual step (same class as Task 208) — **its explicit checklist (skill-review #4/#5):** ☐ capture ONE real `UserPromptSubmit` payload + ONE real `PostToolUse` payload as committed fixtures (the `prompt` / `tool_input.file_path` / `tool_response.content` fields are doc-implied Claude-parity, NOT live-verified; a drift = the wired-but-dead class on prompt-capture/observe); ☐ verify Codex-on-Windows actually LAUNCHES the registered bare-`cmk` MCP server (`.cmd` shim resolution, the D-274 class — Kiro's identical entry works, Codex's Rust launcher may differ; if it fails, re-register `cmd.exe /c cmk mcp serve` on win32); ☐ run `/hooks` trust once + see inject/capture fire in a real chat. Remaining tail: Google Antigravity → gemini-cli → opencode → … on demand. **→ RE-LABELED 2026-07-10 (D-309): Codex's expected slot is now v0.5.2** — v0.5.0 shipped ahead of it (the D-286 re-order), and tagging a 0.4.6 AFTER 0.5.0 would misorder npm `latest`; the "v0.4.6" labels above are preserved as the pre-D-286 expectation per the decision-trail rule. The ORDER stays committed (Codex = the next agent); the digit stamps on cut per D-157._

- [x] 200. _shipped 2026-07-06, PR —_ — **Agent-relative LLM backend — the kit's automatic engine must work WITHOUT Claude Code, by routing through…** → [archive](tasks-archive.md)

- [x] 201. _shipped 2026-07-06, PR —_ — **Split-brain backend — let the user run the automatic memory through a DIFFERENT agent's CLI than the one…** → [archive](tasks-archive.md)

- [x] 202. _shipped 2026-07-06, PR #258_ — **Agent docs + cut-gates for the v0.4.5 agent-relative backend (shipped 2026-07-06, PR #258).** → [archive](tasks-archive.md)

- [x] 197. _shipped 2026-07-02, PR #248_ — **`validate-backlog-triggers.mjs` — the D-248 rule made structural (shipped 2026-07-02, PR #248; D-256).** → [archive](tasks-archive.md)

- [x] 198. _shipped 2026-07-04, PR —_ — **Per-session temporal sweep — the contradiction-catch runs at EVERY existing Haiku maintenance site, not…** → [archive](tasks-archive.md)

- [ ] 199. **→ FIRED-WALK 2026-07-20 (D-372): HALF-FIRED, kept deferred with a REFRESHED trigger.** Its condition was a conjunction — "Task 198 ships AND a live session/cut-gate still surfaces a stale-State answer". **198 has shipped**, so the first half is satisfied and is now removed from the trigger; the second half is an OBSERVATION I cannot make from a terminal (it needs a live session or a cut-gate run). **Refreshed trigger (D-248): a live session or cut-gate surfaces a stale-State answer that Task 198's session-boundary shrink did not catch.** Recording the half-fire explicitly so the next sweep checks only the remaining half instead of re-deriving the whole conjunction. **Write-time temporal DETECTION + in-conversation propose-and-approve resolution (design-first; the Memora-inspired durable design — D-266).** Task 198 shrinks the misleading window to a session boundary; this closes it to ZERO by catching the contradiction at the moment it is born — WITHOUT any hot-path LLM call. **The shape:** at `writeFact` time (auto-extract + `cmk remember`/`mk_remember`), a DETERMINISTIC local check (title-embedding or FTS similarity against existing live State facts — no Haiku, no latency beyond a local index query) detects a probable same-subject predecessor; instead of judging silently, the kit SURFACES it in-conversation via the result envelope (the D-215 mention posture — silent when nothing matches): *"this looks like it updates P-XYZ ('deploys to Vercel', May) — mark it superseded?"* **Claude — already in the conversation with full context — is the judge**, and the user's yes is the gate (the ADR-0018 propose-and-approve shape; strictly better than Memora's silent write-time merge, which discards the moment of change — our close keeps the trail via the 66.2 window-close). Resolution runs the existing `resolveTemporalSupersede` (event-time decides direction, LLM/user never overrides it). **Design must settle before code:** (a) which surfaces carry the mention (mk_remember envelope ✓; the CLI print for `cmk remember`; auto-extract has no conversation — its detections queue for the 198 sweep instead); (b) threshold + at-most-one-mention-per-turn noise budget (the D-169 no-ritual line — never a review queue the user must drain); (c) composition with dedup/near-dup (a detection ≠ a duplicate — dedup fires first, this fires on same-SUBJECT-different-state); (d) whether a declined proposal suppresses re-mention (a cooldown marker per pair). **Done when (design):** ☐ a short design section in design.md §16.18 extending the shipped engine, the four forks above settled; ☐ then the build as its own PR with the automatic-path criterion (the mention appears with NO manual command). · Estimate: M (design S + build S–M) · Depends: Task 198 (the sweep remains the fallback for non-conversational writes) · _**Trigger (D-248): fires when Task 198 ships AND a live session/cut-gate still surfaces a stale-State answer within the per-session window** (evidence the session-boundary cadence is insufficient), OR when the v0.5 learn-loop work touches the write path anyway (ride the surface). Relates D-266, ADR-0018 (propose-and-approve), D-215/D-169 (the mention posture), Memora §3.5/Fig-6 (the write-time precedent — adopted as DETECTION, not silent merge), 66.2 `resolveTemporalSupersede` (the resolution primitive). **➕ Design reference (2026-07-10 sweep, D-308): stash's curator resolves contradictions by appending a dated `## Updates` block (old claim / new claim / which supersedes / why) — never silent overwrite; a lighter-weight sibling of this task's propose-and-approve for the non-conversational (queued) path ([sweep note](../docs/research/2026-07-10-memory-research-sweep.md))._

- [x] 203. _shipped 2026-07-11, PR —_ — **Daily-distill starvation — the automatic distill silently stops on a busy large-corpus repo; BOTH safety…** → [archive](tasks-archive.md)

- [x] 204. _shipped 2026-07-11, PR —_ — **Long jobs must be INCREMENTAL + RESUMABLE-from-artifacts, never all-or-nothing (a cross-cutting…** → [archive](tasks-archive.md)

- [x] 205. _shipped 2026-07-11, PR —_ — **`cmk install` breaks itself on its own running MCP server's DLL lock (Windows) — graceful handling, never…** → [archive](tasks-archive.md)

- [ ] 208. **Complete the Cursor INTERACTIVE cut-gate — the payload-boundary is verified, the full in-IDE §2/§5/§6 run is owed (deferred at the v0.5.0 tag, 2026-07-10; D-307).** The Cursor capture path's THREE bugs found this session are all fixed + merged + live-verified: D-305 (`/c:/` workspace_roots path), D-306 (the UTF-8 BOM breaking `JSON.parse` — the real killer), and the shared `parseHookStdin`. **Verified through the repacked global `cmk` on the EXACT bytes Cursor 3.5.17 sends** (BOM-prefixed `/c:/` payloads) → full bi-turn `now.md` (user=1, assistant=1) + the preference captured — so the CORE capture that was broken is proven at the payload boundary. **What's un-run (deferred, NOT a false-pass):** the full interactive [`cut-gate-cursor.md`](docs/process/cut-gate-cursor.md) §2 Session-1 build + §5 recall + §6 cold-open (R-inject / E1 wedge / E2 privacy / E3 learn-loop) inside a real Cursor IDE session — because the maintainer's free-tier Cursor tokens ran out mid-gate (~14-day refresh). **Done when:** the Cursor gate's §2 (R-cap/R-prompt/R-inject/R-observe fire + a real turn captures a bi-turn `now.md`), §3 (B9 — a `write_source: auto-extract` fact lands from the Cursor session with no manual command), and §6 (E1 the persona wedge injects into a cold-open) all PASS live in a real Cursor IDE session — the same bar Kiro cleared. · Estimate: S (a gate RUN, not a build — the code is done; this is the live-verification the token budget blocked) · Depends: Cursor token refresh (~2026-07-24) · _**Trigger (D-307): fires the moment Cursor tokens refresh** (~14 days from 2026-07-10). NOT a v0.5.0 tag blocker per the user's 2026-07-10 call: the Cursor capture bugs are FIXED + verified at the payload level (D-305/D-306 through the real global bin), the release value (Kiro capture + privacy screen + learn-loop) is done + gated, and the deferral is RECORDED here + in the release note (honest, not silent — the D-292 lie-guard is satisfied because "capture works, verified at the boundary" is TRUE, only the exhaustive interactive gate is pending). Relates D-292 (the 3-agent gate), D-305/D-306 (the fixes this gate-runs), the Kiro §2/§5/§6 that DID complete (the parallel bar), D-307._

- [x] 207. _shipped 2026-07-11, PR —_ — **The Claude-Code hook bins had the same BOM-unsafe stdin parse D-306 fixed for the Cursor/Kiro paths…** → [archive](tasks-archive.md)

- [x] 206. _shipped 2026-07-11, PR —_ — **The per-turn "commit context/ now?" prompt can commit an UNMASKED personal name that's still sitting in…** → [archive](tasks-archive.md)

- [x] 209. _shipped 2026-07-13, PR #288_ — **State-labeled recall — serialize each fact's temporal STATE (`[current]` /…** → [archive](tasks-archive.md)

- [x] 210. _shipped 2026-07-16, PR #296_ — **Deletion-propagation guarantee — a tombstoned/redacted/expired fact must be VERIFIABLY gone from every…** → [archive](tasks-archive.md)

- [x] 211. _shipped 2026-07-14, PR #289_ — **Query state-view gate — a RULE-BASED (wordlist + negation-guard, zero-LLM) classifier tags a search…** → [archive](tasks-archive.md)

- [x] 212. _shipped 2026-07-14, PR #290_ — **Memory-health behavioral dashboard — writes-per-search, empty-search rate, redundant-write rate, snapshot…** → [archive](tasks-archive.md)

- [x] 213. _shipped 2026-07-11, PR —_ — **Provenance pointers through consolidation — every distilled claim in `today-*.md` / `recent.md` /…** → [archive](tasks-archive.md)

- [x] 214. _shipped 2026-07-11, PR —_ — **Name-guard blind spot — `validate-maintainer-name-confined` skips UNTRACKED files, so the manual…** → [archive](tasks-archive.md)

- [x] 220. _shipped 2026-07-10, PR —_ — **Duplicate managed-block blindness in install/uninstall — a second `core-memory-kit:start/:end` block only…** → [archive](tasks-archive.md)

- [x] 224. _closed 2026-07-18, PR #299_ — **SonarCloud scan crashes with `opendir '<workspace>/C:/proj/context'` (exit 3) — INVESTIGATED 2026-07-15: a…** → [archive](tasks-archive.md)

- [ ] 223. **→ PRIOR ART (D-374 sweep): READ [`2026-07-07-auto-judged-privacy-prior-art.md`](../docs/research/2026-07-07-auto-judged-privacy-prior-art.md) (~line 47-53) BEFORE deciding the posture.** Its field-wide finding — *regex is structurally incapable of context-dependent PII (names in prose); no maintained JS library does local NER* — PREDICTED this task's live cut-gate failure 12 days before it was observed, and argues AGAINST adding a deterministic L1 pass for this class. **The L3 privacy judge leaves a real name used as a descriptive LABEL in conversational prose ("the fictional Alex Personname") unmasked — evaluate whether label-context names should also redact (surfaced live in the v0.5.1 cut-gate E2; D-323-adjacent).** **What surfaced:** during the v0.5.1 cut-gate Session-3 cold-open, the E2 privacy check confirmed the target property holds — the git-config EMAIL echoed by `uv init`-adjacent tool output was masked to `«EMAIL»` (0 real emails in the committed transcript; original safe in the gitignored `redactions.log`), and the git-config line's name-echo was truncated before it landed. BUT: two occurrences of the bare name "Alex Personname" survived in the committed transcript — NOT from the session's own PII echo, but from META-PROSE captured because this dev repo dogfoods the kit on the driving session too ("git identity set to the fictional Alex Personname"). **Is this a bug?** ARGUABLY NOT: the L3 judge's instruction (`transcript-screen.mjs` `PII_JUDGE_INSTRUCTIONS`) says redact "personal NAMES of real people" — and "the fictional Alex Personname" reads to the judge as a fictional/example name being DISCUSSED, not a real identity being leaked. That is a defensible reading. **But it's a real judgment-boundary worth deciding deliberately:** a name used as a label/example in prose is a genuinely harder case than a raw `user.name X` echo, and a stricter posture (mask ANY name-shaped token regardless of surrounding "fictional"/"example" framing) trades a few false-positive masks for a tighter privacy guarantee. **Done when (research + decision, small):** ☐ decide the posture — keep the current real-person-only reading (record why), OR tighten the judge instruction to mask name-shaped tokens even in label/example context (accepting the FP cost), OR add a deterministic L1 name-pattern pass for the specific "fictional/example <Name>" shape; ☐ if tightening, one transcript-screen test feeding a "the fictional <Name>" prose entry and asserting `«NAME»`; ☐ note the decision in design §6.10. **Reachability:** the security property (real PII from tool OUTPUT doesn't reach the committed transcript) already HOLDS — this is about prose-label names, a narrower surface. · Estimate: S · Depends: none · _**v0.5.x lane** (privacy-polish; NOT a v0.5.1 blocker — the E2 target property passed, this is a judgment-boundary refinement surfaced by the dogfood-on-the-driving-session artifact). Relates Task 148 / ADR-0019 (the L3 judge), `transcript-screen.mjs` `PII_JUDGE_INSTRUCTIONS`, design §6.10, the v0.5.1 cut-gate E2 (the surfacing run)._

- [ ] 221. **Automated cut-gate parity — grow the live-test harness so the manual cut-gate finds less and less (the user's 2026-07-11 directive + the web-research playbook; D-313).** **The recurring failure mode:** unit-green (2836 passing) + integration-broken — the real bugs (D-303 empty `USER_PROMPT`, D-305 `/c:/` path, D-306 UTF-8 BOM, D-298 cron starvation + HC-10 false-green) surfaced ONLY when a human drove the REAL bins against a REAL agent on a REAL project. Google's small/medium/large taxonomy explains WHY: hermetic unit tests are DESIGNED to be blind to process/agent-boundary phenomena; 2836 green + 4 missed bugs aren't in tension. The kit already has the model (`scripts/live-verify.mjs` drives real `claude -p` through real hooks, dev tree shimmed onto PATH, user-tier isolated), but it only covers the persona wedge. **The prioritized playbook (from the [2026-07-11 research report](../docs/research/2026-07-11-faithful-live-test-automation.md), cheapest+highest-recurrence first):** ☐ (1) **fixture corpus of real captured payloads** per agent×platform — generalize the D-306 fix (a real BOM-prefixed Cursor payload was captured via a live stdin probe and committed as a fixture) into a STANDING corpus fed to the real bins as subprocesses; ☐ (2) **property-based "weird input corpus"** (fast-check) for every hook stdin parser — BOM/CRLF/empty/unicode/oversized; ☐ (3) **"watch the watchmen" tests** — seed a broken state, assert the health check reports UNHEALTHY (the HC-10 D-298 test IS this pattern; generalize to every HC that could false-green); ☐ (4) **fault-inject kill-mid-run** — `spawnSync` + timed SIGTERM to kill a cron/distill at 80% and assert forward progress (the Task-204 resumability property, live — this task's `live-distill-probe` shape made permanent); ☐ (5) **real-OS matrix** running the fixture-replay tests (would've caught `/c:/` structurally); ☐ (6) broaden `live-verify.mjs` past the persona wedge (LOWEST priority — most expensive, live-model-dependent). **Explicit non-goals (from the research):** NO chaos-engineering platform (Litmus/Chaos Mesh overkill for a single-machine CLI); NO exact-match assertions on live LLM output (presence-based is correct, already done). **Done when:** items (1)–(4) ship as `npm run` scripts wired appropriately (the deterministic ones into `npm test`; the live-model ones on-demand like `live-verify`), each mapped to the bug class it prevents recurring; a short `docs/process/live-test-playbook.md` records which harness covers which class. · Estimate: L (staged — (3)+(4) are cheap and land first; (1)+(2) next; (5)+(6) as capacity allows) · Depends: none · _**v0.5.x lane (process/quality investment; the user's standing directive makes it committed, not trigger-gated — "so we don't wait for the cut-gate to find problems"). Staged: the cheap high-yield items ((3) watch-the-watchmen, (4) fault-inject) ride the tasks that surface each class (203/204 already added the first of each); the corpus + property-based + matrix items are their own PRs.** Relates the [2026-07-11 research report](../docs/research/2026-07-11-faithful-live-test-automation.md), `scripts/live-verify.mjs` (the model), D-303/D-305/D-306/D-298 (the bug classes it targets), CLAUDE.md "Live-test toward cut-gate parity" (the binding rule this operationalizes), D-313._
  - ☐ **(7) cut-gate §6 E3 script fix — the positive-dampen path is unexercised (surfaced live in the v0.5.1 cut-gate).** E3's failure-turn (`uv run pytest tests/test_nonexistent.py`) produced NO `trust-signals.log` delta — CORRECTLY, because the learn-loop dampens only the ids the model recently SEARCHED (tight attribution, `judge-signals.mjs`), and the cold-open did ZERO in-chat searches (recall.log: inject 2, search 0), so the failure had nothing to attribute to. The E3 script therefore only ever proves the anti-false-positive guard + the recall-attribution log, never a POSITIVE dampen. Fix: the E3 step must script a **search-then-contradict** scenario — (a) ask a question that triggers an in-chat `mk_search` (logs a `source:search` line), THEN (b) a turn whose outcome contradicts a recalled fact (a failed command in the domain of the searched fact, or a `PREDICTION:MISS`) — so the judge has searched ids to dampen and `trust-signals.log` gets a real delta. Until then, E3 in the cut-gate is honestly "loop-plumbing confirmed, positive-signal deferred." _Provenance: the v0.5.1 cut-gate E3 run; relates `judge-signals.mjs` (the tight-attribution rule), `recall-log.mjs`, Task 194 (the ranking this signal feeds)._

- [x] 222. _shipped —, PR —_ — **Narrow the Task-205 MCP-preflight trigger — it fires on EVERY interactive `cmk install`, but the DLL-lock…** → [archive](tasks-archive.md)

- [x] 216. _shipped 2026-07-10, PR —_ — **Screen-before-committed-write: close the Poison_Guard side doors — a shared `screenBeforeCommittedWrite()`…** → [archive](tasks-archive.md)

- [x] 225. _shipped 2026-07-18, PR #302_ — **`cmk import-sessions` — bootstrap the memory from EXISTING agent session history, so day one isn't empty…** → [archive](tasks-archive.md)

- [x] 226. _shipped 2026-07-19, PR #303_ — **Recall context-expansion + the explicit recall ladder — an "expand" rung between search-hit and…** → [archive](tasks-archive.md)

- [x] 227. _shipped 2026-07-19, PR #304_ — **Recall citation completeness — every recall result carries date + heading + source, `cites` gets wired or…** → [archive](tasks-archive.md)

- [ ] 228. **Proactive-memory connectors — GO/NO-GO research ADR: does the kit ingest context that never passed through a chat? (the OpenWiki Brains class; from the 2026-07-12 market sweep; D-326).** **The question:** LangChain's OpenWiki Brains (2026-07-10) frames built-in memory as REACTIVE (remembers what you tell the agent / what it infers from conversations — the kit's class) and adds PROACTIVE memory: connectors (Gmail, Notion, git, X, HN, web search) pull external context into a local markdown wiki on a schedule. Their roadmap explicitly names "Claude/Codex local sessions" as a future connector — i.e. they are moving INTO the kit's territory; this ADR decides whether the kit moves the other way. **The honest starting position (record it in the ADR either way):** the clearly IN-THEME cut is **other-agents' local session histories** — which Task 225 already covers (Claude Code) and the cross-agent adapters extend naturally (Kiro/Cursor/Codex session stores as import sources = a 225 tail, not a new paradigm). External SaaS connectors (Gmail/Notion/X) are a NEW security surface (credentialed ingestion + third-party content straight into committed tiers) and likely scope creep for a local-first coding-agent kit — but decide it deliberately, not by drift. **Done when (research + ADR only, no build):** ☐ survey the field (OpenWiki's connector architecture + at least 2 others); ☐ the ADR gives a per-connector-class verdict (agent-session stores / git-derived / external SaaS) with the security analysis (every ingested byte must route through screenBeforeCommittedWrite + the privacy judge — non-negotiable); ☐ a GO on any class spawns a build task laned on cut; a NO-GO records the named re-visit trigger. · Estimate: M (research) · Depends: none · _**v0.7.0 rider (D-326)** — runs alongside the team-layer headline (both are "memory beyond your own sessions" breadth). Relates Task 225 (the in-theme cut it generalizes), Task 174 (git-history backfill — a proactive connector the kit already planned), the [2026-07-12 sweep note](../docs/research/2026-07-12-day-one-memory-market-sweep.md), D-326._

- [x] 229. _shipped 2026-07-19, PR #305_ — **Speak the memory-taxonomy vocabulary — map the kit's surfaces to working/semantic/episodic/procedural in…** → [archive](tasks-archive.md)

- [x] 230. _shipped 2026-07-16, PR #294_ — **`cmk install` (re-run / update) must REFRESH kit-owned scaffold to the current version — today it skips…** → [archive](tasks-archive.md)

- [x] 231. _shipped 2026-07-15, PR #293_ — **Screen-then-mask: the invisible-unicode Poison_Guard is DEFEATED on the default write paths because…** → [archive](tasks-archive.md)

- [x] 239. _closed 2026-07-20, PR —_ — **RETRACTED (D-366): the "concurrent scratchpad writes silently lose bullets" report — NOT a bug (all bullets accounted for; the serial baseline falsified it).** → [archive](tasks-archive.md)

- [x] 242. _shipped 2026-07-20, PR #312_ — **Auto-extract fails SILENTLY to zero under load — SELF-HEAL it (D-369; the user's "how do we make this…** → [archive](tasks-archive.md)

- [x] 256. _shipped 2026-07-23, PR #324_ — **Anchor co-citation edges — "graphify for memories": densify the graph deterministically from the citation…** → [archive](tasks-archive.md)

- [ ] 255. **The kit's own memory viewer — D-121 finally gets built (the user's 2026-07-23 call: option 3 of D-397, "we had it for 6 weeks and nothing… now i want to be decisive and just do it").** **The lineage:** v0.1.0 shipped a lying `cmk view` stub → D-121 (2026-06-11) removed the stub, kept the idea as a v0.4 design-first product question → Pulse (15.8k★) + claude-mem's shipped viewer recorded as demand evidence (2026-06-12) → parked untouched for 6 weeks. **The shape (design-first, then build — D-121's own requirement):** ☐ a DESIGN/GRILL session with the user BEFORE code (grilling skill; the questions: audience — the README's non-developer user; read-only vs write; localhost server vs static export; what the kit can show that Obsidian can't — trust tiers, supersession chains via 232's edges, doctor status, conflict queues, the recall fire-rate from 233's logging); ☐ then the build task-list falls out of the design. Constraints already settled elsewhere: read-only FIRST (the safe write path stays the only writer — ADR-0018 class); no heavy new deps (the kit is a zero-native-dep CLI outside better-sqlite3); Task 254's Obsidian view is the COMPANION not the competitor — 254 tests whether people look at their memory, 255 owns the kit-specific rendering. **Done when (design phase):** the grill happened, a design doc section exists (design.md §, not a standalone), and the build sub-tasks are filed with estimates. · Estimate: design S, build M–L · Depends: 232 (edges) + 233 (fire-rate log) shipped; 254 informative but not blocking · _**LANED v0.6.4 as the HEADLINE differentiator (D-397)** — the one-differentiator-per-minor rule: v0.6.3 stays coerce-the-hope-grade-surfaces; the viewer is its own minor. Relates D-121 (the parked idea, now committed), D-397, Task 254 (companion), the [PAI/Pulse note](../docs/research/2026-06-12-pai-miessler-paradigm-ally.md) (demand evidence), claude-mem (the 77k★ precedent)._

- [x] 254. _shipped 2026-07-23, PR #323_ — **Obsidian vault view — the memory tier as a browsable human UI, near-zero build (the user's call…** → [archive](tasks-archive.md)

- [ ] 253. **Three shipped-precedent borrows from obsidian-mind — injection-size meter, source-aware injection split, tmpdir-cwd for detached spawns (filed 2026-07-23 from the [2026-07-21 note](../docs/research/2026-07-21-repo-obsidian-mind.md)'s borrow list; the other two borrows already shipped in 232/233).** ☐ **(a) Source-aware injection split** — read the SessionStart hook payload's `source` field (`startup`/`clear` vs `resume`/`compact`): on resume/compact, replace unchanged static snapshot bulk with a one-line "unchanged, already in context" pointer instead of re-injecting (their `session-start.ts` mode split — a real token saver; verify our hook payload exposes `source` reliably first); ☐ **(b) injection-size meter** — one appended line reporting the byte cost of each injection (their `formatInjectionSize`; cheap Door-5-grade transparency on the snapshot surface); ☐ **(c) tmpdir-cwd for detached spawns** — pin fire-and-forget children's cwd to `tmpdir()`, never the working tree (a named Windows technique against stale-handle races — squarely our documented spawn-pain class; sweep our detached-spawn sites). Each is independent; ship subsets as riders. · Estimate: S each · Depends: none · _**Trigger (D-248): (a)+(b) ride the next injection/snapshot-surface task; (c) rides the next spawn-layer task or the first stale-handle flake, whichever first.** Relates the obsidian-mind note (borrow candidates 1/3/6), design §7.1 (injection budget), the Windows spawn-pain precedents, D-396._

- [ ] 251. **OKF interop — the kit's memory as a standard Open Knowledge Format bundle (filed 2026-07-22 from the OKF research note; DEFERRED with trigger).** **The finding:** Google Cloud shipped OKF v0.1 (`GoogleCloudPlatform/knowledge-catalog`, `okf/SPEC.md`, primary-verified) — knowledge as markdown-with-YAML-frontmatter, required `type`, reserved `index.md`/`log.md`, markdown links as the graph, git-native, permissive consumers. The kit's fact files are ~90% OKF-shaped already (see the isomorphism table in the [research note](../docs/research/2026-07-22-okf-google-open-knowledge-format.md)). **The opportunity:** interop as an adoption lever — ☐ a frontmatter compatibility mapping (kit schema ↔ OKF fields; likely zero schema change, a doc table + maybe aliasing on export); ☐ `cmk export --okf` (emit `context/memory/` as a valid OKF bundle: index.md, log.md from DECISIONS, links from `related:`); ☐ optionally `cmk import` of an external OKF bundle → searchable memory (the reverse seam; reuse import-anthropic's shape). Positioning: "your memory IS a standard OKF bundle." **Why DEFERRED, not laned:** OKF is v0.1, day-one, zero external consumers beyond its own reference agent — interop with a spec nobody consumes yet is speculative work; the spec may churn. · Estimate: S (mapping doc) + M (export/import) · Depends: none · _**Trigger (D-248): OKF ships a v0.2+/1.0 OR any non-Google OKF consumer/producer appears in the wild OR a user asks for OKF export/import once** — whichever first; per the unconditional prior-art rule, re-read the OKF repo fresh at build time. Relates the [OKF research note](../docs/research/2026-07-22-okf-google-open-knowledge-format.md), SOURCES.md entry, ADR-0023 (OKF's link-graph independently confirms the deterministic shape), Task 232 (edges-from-markdown — the export's `related:`→links source), D-391._

- [ ] 250. **Failure-driven health nudge + a troubleshooting skill — make the kit tell the AI when something's broken and how to fix it (the user's design, 2026-07-22).** **The gap (`U-U5PPSG7Y`, high trust):** users won't run `cmk doctor`; silent failures must surface automatically. Today doctor is reactive-only — no skill, no trigger — while memory-write/search have both. **REJECTED shapes (recorded so nobody re-proposes):** ☒ auto-run doctor at SessionStart (HC-10 cut — ceremony); ☒ a SessionStart status-line nudge (fires every session + NO self-clean lifecycle: after one fire it can only know the fix landed by re-checking every session = the auto-run we cut). **The shape (self-cleaning by construction):** the nudge is FAILURE-DRIVEN, anchored to the EVENT not a schedule. The kit already writes its own failure signals (`extract.log`, spawn-error entries, `audit.log`, `poison-guard.log`); the nudge fires ONLY when a FRESH failure exists there — fix it → next run succeeds → no fresh signal → no nudge. **Build (two halves):** ☐ extend `buildMemoryHint` (capture-prompt.mjs — the existing per-prompt hint) with a CHEAP fresh-failure check (log tail / marker file, NOT a doctor run — hot path); whisper '⚠ a kit op failed — the troubleshooting skill can diagnose'; ☐ a NEW troubleshooting skill (scaffolded like memory-write/search) that teaches `cmk doctor` + repair/reindex/forget on the whisper or a recognized symptom, extensible to other cmk verbs. **Prior art: NONE** — D-374 corpus gap 'self-healing CLI repair UX'; **outward research + a grill REQUIRED before building** (D-375). **→ PRIOR-ART UPDATE 2026-07-22 (D-393): the gap PARTIALLY closed** — the [Octopoda-OS code dive](../docs/research/2026-07-22-octopoda-os-code-dive.md) supplies real, code-verified detection prior art: 10 deterministic loop classifiers (pure functions over an event log — the shape 212/250 want), and most on-point, reflection.py's **pattern-aware false-positive suppressor** (recognizes known-benign key shapes — rolling summaries, heartbeats, recovery markers — and suppresses the alert), which is a shipped precedent for THIS task's noise-threshold amendment. Still missing from the corpus: the self-heal-first repair UX half (Octopoda detects + pauses; it does not repair) — the grill still owes outward research there. **TWO AMENDMENTS from the 2026-07-22 review (bake into the grill's frame):** (1) **self-heal-first scoping (U-LNFPaXTT: self-heal first, report last)** — as first filed this task was ALL report, contradicting the kit's own recorded lesson AND the user's 248 principle ('doctor should only be part of the fix, not the actual fix'). The hierarchy: failure classes the kit can safely fix itself get FIXED automatically (the 248 model); the whisper covers ONLY what genuinely needs judgment (missing agent CLI, broken hook registration, a reinstall decision). (2) **noise threshold** — a single transient failure (one Haiku timeout that the next run recovers from) is non-actionable noise; the whisper must key on ACTIONABLE failure (repeated, or failed-without-subsequent-success), never raw fresh-failure-exists. Also honest scoping: the WHISPER is the load-bearing half — this session proved skills alone under-fire (memory-search: zero fires); the skill without the whisper is a shelf ornament. · Estimate: M · Depends: none (pairs with reframed 248) · _**v0.6.3+ lane** (design/grill/research first). Trigger (D-248): laned v0.6.3; needs the outward-research + grill step before code. Relates U-U5PPSG7Y, D-169/D-164/D-85 (surface-automatically lineage), P-G22QZQL2 (health-behind-a-command relocates a failure), Task 248 (the sibling install-flow auto-recover), the D-374 corpus gap, `buildMemoryHint`._

- [ ] 249. **Archive the append-only histories — the living docs are getting too big to read whole (the user's call, 2026-07-22; measured).** **The data (living docs, KB / lines):** `DECISION-LOG.md` **1.1 MB / 2173** · `tasks.md` **888 KB / 1983** · `build-log.md` **507 KB / 3065** · `design.md` **429 KB / 3777**. **The key fact that scopes this:** NONE of these are auto-injected into agent context (verified — `inject-context.mjs` references none of them), so size does NOT bloat the SessionStart snapshot; the cost is (a) the agent's context budget when it reads them at task-start, (b) human navigability, (c) git-diff noise. So this is real but not urgent. **What to archive (the clean wins — append-only / completed content, pure relocation, zero information loss):** ☑ **`tasks.md`** — split COMPLETED tasks (with their large shipped-note retrospectives) into `specs/tasks-archive.md`; the live file keeps OPEN tasks + a one-line pointer per shipped task (id · title · `_shipped_` date · PR). This is the biggest win — roughly halves the working file. ☑ **`DECISION-LOG.md`** + **`build-log.md`** — split at a version/date boundary (e.g. pre-v0.5 → `docs/journey/DECISION-LOG-archive-pre-v0.5.md` / `build-log-archive-*`), recent stays live. Append-only + newest-at-top makes the cut clean. **Explicitly OUT of scope (recorded so nobody expands it):** ☒ **`design.md`** — the Spine (D-228: ONE canonical mechanism doc, never a standalone `docs/design/`); its size is comprehensiveness, not cruft. If touched at all it is section-level pruning of shipped `§16 v0.1.x candidates`, a SEPARATE careful review — never an archive split. ☒ **`INDEX.md` (436 KB)** — that is the DOGFOOD MEMORY TIER at ~1,949 facts (the reindex 'consider consolidation' warning), a memory-curation problem (Task 95 dream re-curation territory), NOT doc archiving; do not conflate. **The care this needs (why it is a TASK, not an impulsive split):** the archived files become FROZEN RECORDS (the source-of-truth table's point-in-time-records list — exempt from the drift walk + the counts validator by path prefix); the live docs' cross-references (`D-nnn`, `Task N`) must still resolve after the split (the `references` validator is the gate, and Task 247's `D-nnn` check would cover the log split); `DOCUMENTATION-MAP.md` must register the new archive files (the `registry` validator). **Correction 2026-07-28 (Task 247 shipped):** those two claims — that 247's check "would cover the log split" and that it "composes but does not block" — were only true in intent; as first built, 247 read a single `DECISION-LOG.md`, so the archive split would have dangled every pre-split `D-nnn` on the day it landed. 247's review round fixed it at the source: the anchor sources are now GLOBS — `docs/journey/DECISION-LOG*.md` and `specs/tasks*.md` — parsed per-file and unioned, so `DECISION-LOG-archive-*.md` and `tasks-archive.md` keep supplying their ids the moment they exist (design §17.13; locked by two tests). **One thing this task still owes:** `docs/journey/` is already a frozen-record path prefix, but `specs/` is NOT — so `specs/tasks-archive.md` needs an explicit frozen-record entry in `validate-docs`'s list, or its historical `D-nnn`/count claims become policed as live prose the day it appears. **Done when:** ☑ `tasks.md` holds only open tasks + shipped-pointers, completed detail in `tasks-archive.md`; ☑ DECISION-LOG + build-log split at a stated boundary, archives registered as frozen records; ☑ every internal reference still resolves (`npm test` green, all doc validators); ☑ the source-of-truth table + frozen-records list name the new archive files. **BUILT 2026-07-28 — the three boundaries, stated:** (1) **tasks.md** — all **219** completed (`[x]`) parent entries relocated byte-identical to [`tasks-archive.md`](tasks-archive.md) in source order, each replaced in place by `- [x] NNN. _<verb> <date>, PR #N_ — **<title>** → [archive]`; the **38** open/partial entries are untouched, and all **257** ids remain in the live file so `validate-numbering-gaps` sees an unbroken sequence. A level-3 sub-section that belongs to an entry (`### 38a.`, the `### Post-PR-31 audit campaign tracker` under Task 23) travelled WITH its entry; level-2 section headings and their prose stayed live. Where an entry carried no date/PR, the pointer prints `—` rather than guessing. **918 KB / 2,001 lines → 194 KB / 768 lines (−79%).** (2) **DECISION-LOG** — cut at the release boundary: **D-1…D-306 archived, D-307 onward live**, because D-307 IS the "tag v0.5.0 now" decision. **1.15 MB / 2,353 → 313 KB / 1,191 lines (−73%).** (3) **build-log** — cut at a top-level section boundary: **§0–§9 archived, §10 onward live.** Deliberately archived LESS than "everything pre-v0.5" (the explicitly-permitted option): §10b…§10i are the per-version arcs and they are **interleaved, not chronological** — §10d is split in two around §10e and §10f spans v0.4.3→v0.5 — so cutting inside that block would break the narrative; §11 "How to extend" stays live as required. **524 KB / 3,081 → 165 KB / 295 lines (−69%).** The owed frozen-record entry is paid (`specs/tasks-archive.md` in `validate-docs`'s `FROZEN_RECORD_PREFIXES`, red-then-green tested); `docs/journey/` already covered the other two by prefix. Relative links survived unchanged because each archive sits in the SAME directory as its source (same resolution base) — verified by the `references` family over all 2,234 markdown files. · Estimate: M · Depends: none (247's D-nnn check composes but does not block — **true as of 2026-07-28**, once 247 shipped the glob'd anchor sources; see the correction above) · _**v0.6.3 lane** (doc-hygiene, batches with 247/248). Trigger (D-248): laned v0.6.3; pull forward if a doc crosses ~1.5 MB or an agent's task-start read is context-pressured by doc size. Relates D-228 (design.md-is-Spine, the out-of-scope guard), Task 95 (the INDEX.md/memory-curation problem this is NOT), the source-of-truth table + frozen-records list, `validate-docs` references/registry/counts families._

- [x] 247. _shipped 2026-07-28, PR #328_ — **`validate-docs` doesn't check `D-nnn` decision-log citations — the one internal-reference family on the…** → [archive](tasks-archive.md)

- [x] 257. _shipped 2026-07-27, PR #327_ — **`frontmatter.parse` is BOM-blind — a `<U+FEFF>`-prefixed fact file is invisible to the ENTIRE kit (found…** → [archive](tasks-archive.md)

- [x] 248. _shipped 2026-07-25, PR #326_ — **Install/update AUTO-RECOVERS pre-existing orphaned memory tiers — the fix lives in the flow, doctor is…** → [archive](tasks-archive.md)

- [x] 246. _shipped 2026-07-22, PR —_ — **The capture hooks write to `process.cwd()` — a subdirectory cwd silently forks the memory tier (found…** → [archive](tasks-archive.md)

- [x] 245. _shipped 2026-07-21, PR —_ — **`npm install -g` appeared to no-op from inside the repo directory — OBSERVED live, cause NOT yet diagnosed…** → [archive](tasks-archive.md)

- [ ] 244. **Pack-trust assessor — let recall say "I'm not confident enough to answer from this" (the ONE genuinely new idea from the 2026-07-21 research pass; D-380).** **What the kit does today:** recall returns the best-scoring facts it has. Trust exists per-FACT (`trust_score`, the 151.T/194 lineage; the search rank blends it once a fact has ≥3 outcome signals) — but there is **no assessment of the PACK as a whole**, and no path for the agent to abstain or escalate. A query whose best hits are three medium-trust, mutually-unrelated, six-month-old facts returns them with exactly the same confidence posture as one whose top hit is a high-trust, corroborated, recent fact. The agent has no signal that the second answer is worth more than the first.
  **The borrow (Mnemo, hackathon MemoryAgent — [note](../docs/research/2026-07-21-mnemo-qwen-memoryagent.md)):** a runtime **assessor** scores the retrieved pack before the agent answers from it, and can route to abstain / escalate / answer-with-caveat. Their pack-trust rule is a MINIMUM (the pack is only as trustworthy as its weakest load-bearing member), not an average — an average lets one strong fact launder three weak ones. **This has no analog anywhere in the kit or the backlog** (verified against Tasks 194/151.T/212/233 and design §9.4 — all per-fact or per-query-rank, none pack-level).
  **The second, harder half (also Mnemo's, and the reason their v3→v4 fix was a SCORING bug not a model bug): the metric must be two-sided.** Unsafe-acceptance alone is gameable — a system that abstains constantly scores perfectly on it. Any gate here ships with its **over-abstention counter-metric**, and both semantics are written down BEFORE the first number is computed. This is the same discipline D-124/§17.10 applies to budgets (test the edges, not the middle), applied to a judgment gate.
  **Open design questions (grill before building — this is deliberately NOT specced yet):** ☐ where does it run — inside `cmk search`'s result assembly, in the memory-search skill's ladder, or as an MCP-tool response field the model reads? ☐ is the verdict a number, a band (high/medium/low), or a sentence the agent reads? ☐ does it gate anything, or is it purely advisory (the kit's fail-open posture and the ADR-0017 "judgments never auto-rank" line both push hard toward ADVISORY)? ☐ what feeds it — trust_score minimum, recency spread, corroboration count, score gap between hit 1 and hit 5, or the recall-log's own miss history? ☐ how does it compose with Task 233's gated pointer-hint (that decides WHETHER to hint; this decides HOW MUCH to trust what came back — adjacent, not the same gate).
  **Done when:** ☐ the assessor is a PURE function over a result set (testable without a corpus, per the boundary-test discipline); ☐ both metrics defined in writing before implementation, with the over-abstention counter-metric asserted by a test that FAILS a constantly-abstaining assessor; ☐ advisory-only unless a grill session decides otherwise (never silently suppresses a result); ☐ a real-corpus check on this repo's ~1,880 facts showing the score separates a known-strong query from a known-weak one. · Estimate: M (S to implement, M because the metric design is the actual work) · Depends: none (composes with 233 if that lands first) · _**NOT LANED — needs a grill session first** (the design questions above are genuine forks, not implementation details; laning it before that would be the fabricated-commitment shape D-267 warns about). **Trigger (D-248):** lane it when EITHER (a) the recall-surface lane (232/233) ships and the ladder is stable enough to hang a confidence signal off, OR (b) a live session shows the agent answering confidently from a demonstrably weak pack (the failure this prevents — file it the moment it's observed). Relates D-380, [the Mnemo note](../docs/research/2026-07-21-mnemo-qwen-memoryagent.md), Task 194 (per-fact outcome signals — the input this aggregates), Task 212 (the stats surface where its metrics would live), ADR-0017 (judgments never auto-rank — the constraint on making it a gate)._

- [x] 243. _shipped 2026-07-21, PR —_ — **Drop the deprecated `prebuild-install` from the user's install output — a one-dependency bump, NOT the…** → [archive](tasks-archive.md)

- [x] 241. _shipped 2026-07-21, PR —_ — **De-duplicate the fact-store walk + 3 copy-pasted helpers (measured clone audit; D-368).** → [archive](tasks-archive.md)

- [x] 240. _shipped 2026-07-21, PR —_ — **CI toolchain drift — Node is pinned in 9 places and has already drifted (the ONE surviving ECC process…** → [archive](tasks-archive.md)

- [ ] 238. **Agent-proximity collision advisory — parallel agents on one repo (the ECC borrow with the LONGEST fuse; D-364, premise CORRECTED 2026-07-20 by D-365).** **What they built** (`scripts/lib/agent-proximity/`, verified): a TCAS-style advisory that steers two concurrently-running agents off the same files — collision risk `R∈[0,1]` via noisy-OR over three channels (edit-overlap by Szymkiewicz–Simpson over line ranges, dependency-graph coupling by BFS with γ-decay, tree proximity), then `advise()` emits steer/hold. *(Their "vector-space view" comment is a red herring — coordinates are deterministic path-segment hashes, not embeddings.)* **Original rationale (2026-07-20, WRONG — preserved per the decision-trail rule):** *"the kit has no parallel-agent surface today — one agent, one repo, one `context/`… the kit's own lock discipline (`.locks/`) already serializes concurrent WRITES."* **DISPROVED the same day (D-365):** the user asked *"can't you test that? run the next task with agents like if I enable ultracode+workflow and see what happens?"* — the live probe found (a) the parallel surface is **already reachable** (workflow fan-out, two Claude Code windows, the shipped Claude+Kiro dual-agent config), and (b) `.locks/` does **NOT** serialize scratchpad writes at all — `appendScratchpadBullet` takes no lock, and 16-way concurrency silently lost 8 bullets (→ **Task 239**, laned v0.6.0). The "no problem yet" premise was reasoning where a 20-line probe was available; the trigger had effectively already fired and nobody had looked. **Corrected disposition:** the *data-loss* half is now Task 239 (real, laned, current cut). What remains HERE is only the genuinely-forward-looking half — **semantic** collision advice (two agents editing overlapping code/files, not corrupting one memory file), which is still speculative for a memory kit. **Trigger (D-248, refreshed + narrowed): when the kit ships a feature that itself orchestrates 2+ agents against one repo AND Task 239's serialization proves insufficient — i.e. agents no longer lose data but still duplicate or contradict each other's WORK.** Re-checked at every minor's backlog sweep (D-267 fired-walk), now with the lesson that a trigger's condition must be *probed*, not assumed. **If it fires, the build is:** ☐ port the three-channel risk score against the kit's own edit-observation stream; ☐ advisory-only output (never block — the kit's fail-open posture); ☐ tests pinning steer/hold at the risk thresholds. · Estimate: L · Depends: a concurrent-agent surface existing · _**NOT LANED — trigger-gated by design** (the honest answer: no version, because no problem yet; a lane here would be a fabricated commitment, D-267). Relates D-364, the ECC study [§5.5](../docs/research/2026-07-20-ecc-harness-os-comparison.md), the `.locks/` serialization that covers today's real contention._

- [x] 237. _shipped 2026-07-21, PR —_ — **Supply-chain watch — a standing check for compromised deps in the kit's own dependency surface (the ECC…** → [archive](tasks-archive.md)

- [x] 236. _shipped 2026-07-20, PR —_ — **Catalog/registry CI gate — assert the shipped catalog matches the tree (the ECC borrow that validates Task…** → [archive](tasks-archive.md)

- [x] 235. _shipped 2026-07-20, PR —_ — **`PreCompact` capture — bank the session at the compaction boundary, not only at SessionEnd (the ECC…** → [archive](tasks-archive.md)

- [x] 234. _shipped 2026-07-20, PR #311_ — **Stale-replay guard on the injected snapshot — separate DURABLE KNOWLEDGE from STALE WORK-STATE (the…** → [archive](tasks-archive.md)

- [x] 233. _shipped 2026-07-22, PR #322_ — **The recall-trigger hybrid: gated cheap-index pointer hint + existence advertisement + the in-skill…** → [archive](tasks-archive.md)

- [ ] 252. **`maskPii` email-pattern hardening — dotted local-parts slip the screen (found by Task 233's review round, 2026-07-22; PRE-EXISTING kit-wide gap, not a 233 regression).** **The gap:** `maskPii` masks `bob@corp.io` → `«EMAIL»` but does NOT match dotted local-parts (`alice.leak@x.com`) — every disk write that relies on the L1 screen (transcript capture, hint telemetry, any future prompt-derived write) inherits it. Found when a review-round PII test passed for the wrong reason (tokenization split the dots) and was strengthened honestly. **The build (S):** ☐ audit `pii-patterns`/`maskPii` against a real-world email corpus shape (dots, plus-addressing, subdomains) + the other pattern classes for sibling gaps; ☐ fix the regex(es); ☐ regression tests per shape incl. the exact `alice.leak@x.com` case; ☐ verify no over-match (an `@` in code/log text that is not an email). · Estimate: S · Depends: none · _**Trigger (D-248): next privacy-surface task OR the v0.6.3 cut, whichever first** — cheap enough to ride any privacy-adjacent lane; the gap is live on every capture today. Relates Task 233 (the finding), FR-15/design §6.6 (the screen contract), D-395._

- [x] 232. _shipped 2026-07-22, PR #321_ — **Activate the graph edges the kit already writes — `cmk links` / `mk_links` + the rebuildable edges table…** → [archive](tasks-archive.md)

- [ ] 217. **Harden the delete-guardrail — it's regex-only and narrower than its own D-192 threat model (from the 2026-07-10 security review; D-312).** **The gap (`guard-memory.mjs`):** the guardrail stops the LITERAL shell command it was built against but not same-effect alternatives — (a) **language-level deletes** reachable through the same Bash/PowerShell tools: `python -c "import shutil; shutil.rmtree('context/memory')"`, `node -e "require('fs').rmSync('context/memory',{recursive:true})"`, `os.remove`/`Path.unlink`/`ri` (PowerShell alias) all pass `touchesMemory` but fail `isDestructive`; (b) **the `Write`/`Edit` tool is not gated at all** — the guard wires only to `PreToolUse` for shell tools, so a `Write` with empty content truncates a memory file with no block (the async `observe-edit` fires AFTER); (c) **encoded/obfuscated commands** (`-EncodedCommand`, `iex`, `base64 -d | sh`) bypass BOTH the verb and path match since neither appears as a literal; (d) **`.claude/settings.json` isn't in `MEMORY_TOKENS`** — deleting the guard's own hook config disables it silently. **Design-first (this is a threat-surface expansion, not a one-line fix — get the over-block/under-block balance right per the guard's own "over-block is safe, under-block is data loss" philosophy):** ☐ extend `DESTRUCTIVE` with the language-level delete primitives (rmtree/rmSync/unlink/ri); ☐ add a `PreToolUse` gate for `Write|Edit|MultiEdit` whose resolved path is under a memory tier AND whose result is empty/near-empty vs the original; ☐ treat encoded-command markers as suspicious-by-default (block/confirm regardless of token match — an unreadable payload shouldn't get a free pass); ☐ add `.claude/settings.json`/`.local.json` + installed hook-script paths to `MEMORY_TOKENS`. **Done when:** each bypass class above has a test that now BLOCKS (or confirms), and the false-positive surface is measured (an ordinary `python -c` / `Write` to a non-memory path is NOT blocked). · Estimate: M–L (design the balance + implement + a real bypass-corpus test) · Depends: none · _**v0.5.x lane** (security depth; NOT a v0.5.1 blocker — the guard is DEFENSE-IN-DEPTH over the primary git-recoverability + audit-trail, and the bypasses need local shell access already; but real gaps against the stated model, so a near-term security slot). Relates D-192/D-193 (the guard's origin + the structural PreToolUse guardrail), `guard-memory.mjs`, the 2026-07-10 security review (findings 1/2/7/8), D-312._

- [x] 218. _shipped 2026-07-13, PR —_ — **MCP server index freshness — the long-lived server reindexes once at boot and never refreshes for…** → [archive](tasks-archive.md)

- [x] 219. _shipped —, PR —_ — **Add `busy_timeout` to `openIndexDb` — concurrent writers get an immediate `SQLITE_BUSY` instead of a…** → [archive](tasks-archive.md)

- [x] 215. _closed 2026-07-18, PR —_ — **The nightly `cmk-daily-distill` schtask pops a visible `node.exe` console window over the user's screen on…** → [archive](tasks-archive.md)

- [x] 165. _shipped 2026-07-13, PR —_ — **Two PARKED Kiro-surface gaps found in the live cut-gate (2026-06-23) — NOT v0.4.0 blockers, but real.** → [archive](tasks-archive.md)

- [x] 163. _shipped 2026-06-20, PR —_ — **Ceiling-free compress callers used the hook-sized 50s timeout → needless `haiku_timeout`, stale…** → [archive](tasks-archive.md)

- [x] 152. _shipped 2026-06-15, PR —_ — **`validate-index-completeness` — make the hand-maintained doc indexes structural (surfaced by the…** → [archive](tasks-archive.md)

- [x] 137. _shipped 2026-06-11, PR —_ — **Close the gate-vs-automation gap (the user's 2026-06-11 directive: the 4 gate-found bugs must become…** → [archive](tasks-archive.md)

- [x] 138. _shipped 2026-06-11, PR #165_ — **Import provenance: canonical comment via the shared writeBullet (cut-gate9 F-13 find — bug #5 of the gate…** → [archive](tasks-archive.md)

- [x] 139. _shipped 2026-06-11, PR #166_ — **EMERGENCY: real-Node parse guard + CRLF-tolerant memory reads (cut-gate9 H1 finds, bugs #7 + #8 of the…** → [archive](tasks-archive.md)

- [ ] 127. _(postponed past v0.4 — Kiro/cross-agent first, the user 2026-06-12, D-127. Design-time prior art: caura-memclaw — the 2026-06-12 research note; the live fork is git-native federation vs a memclaw CONNECTOR as the team seam.)_ **The team layer — a COMPANION project in the kit's philosophy (v0.4 candidate; the user's direction 2026-06-10, D-119).** The creator's June roadmap is a central Postgres+pgvector brain with row-level security. The kit's answer stays git-native + local-first: the committed `context/` already IS the per-repo shared brain (versioned, PR-reviewable, no server). What's missing is per-person/per-client scoping ACROSS projects — to be built as a **separate complementary project** (own repo), connected to the kit through a seam, so the kit core stays dependency-light. Open design: the seam shape (an MCP peer the kit's `mk_search` can federate to / a sync target like the Task-72 persona-sync git pattern / a `--scope remote`), the ACL model, and what the companion stores. Slot into the v0.4 breadth discussion — design first, no code before the seam is settled. **→ RE-LABELED 2026-07-10 (D-311): v0.4 closed without the discussion — the companion-project design stays deferred; trigger: a REAL team/multi-user demand signal (a user asks for shared memory ≥2×), OR the v0.6 planning boundary — whichever first.** **→ TRIGGER FIRED + LANED 2026-07-12 (D-326): BOTH trigger halves are now true** — the v0.6 planning boundary is here (the D-326 laning session), AND the market demand signal landed (the origin creator's v3 shipped a TeamOS beta — one shared brain, owner-tagged memories, per-query filtering by who's asking, system/team/client/private levels — his community is beta-testing it now; GBrain lineage). **Verdict: the v0.7.0 HEADLINE** (team = a real differentiator, one-per-minor). Design-first stands (the seam shape / ACL model / companion-vs-in-kit fork from D-119 is still the open design); Task 228 (proactive-connectors ADR) rides the same lane. The TeamOS beta's shape (owner tags + query-time filtering over ONE brain) is a design input CONTRASTING the memclaw central-Postgres shape — the kit's git-native answer must pick its seam against both._
  - Estimate: L (design doc first) · Depends: v0.3.0 shipped

- [x] 149. _shipped 2026-07-19, PR #308_ — **Recall-trigger architecture study — CLONE the real repos, don't trust the note summaries (D-153 follow-up;…** → [archive](tasks-archive.md)

- [x] 125. _shipped 2026-06-10, PR #153_ — **Post-#152 feedback batch (the user's four asks, 2026-06-10).** → [archive](tasks-archive.md)

- [x] 124. _shipped 2026-06-10, PR #154_ — **🐛 `cmk forget` leaves `INDEX.md` stale (dogfood-found, D-112).** → [archive](tasks-archive.md)

## Road to 1.0 — video parity (the "worth giving to a friend" bar)

_Per D-24/D-25/D-26 (DECISION-LOG 2026-06-01). The bar for handing it to a friend = everything the Simon Scrapes video described ([source](../docs/sources/simon-scrapes-master-claude-memory.md)). Cadence: **one wow → live test → publish a version → next**; never shelve until "finished." 1.0 is a **feel** call (the user), not a date._

**Video-layer parity scorecard** (the concrete done-bar):

| Video layer | Status | Gap → task |
| --- | --- | --- |
| L1 — in-repo location (`context/`, travels with git) | ✅ built | — |
| L2 — granular per-fact archive + INDEX | ✅ built | — |
| L3 — bounded scratchpads (hot state, caps) | ✅ built | — |
| L4 — memory-aware hooks (Stop capture / SessionStart inject / auto-extract) | ✅ built | — |
| L5 — **memsearch / semantic Recall** | ⚠️ **keyword FTS5 only** | **Task 65 (the one true parity gap)** |
| L6 — cron self-curation (distill / curate) | ✅ built | — |
| Frozen-snapshot inject | ✅ built | — |
| _Beyond video — cross-project persona FILLS_ (our wedge, D-27) | ⚠️ F2-broken | **Task 64** |
| _Beyond video — temporal currency_ (facts stay true as they age) | ⚠️ not built | **Task 66** |

**Release sequence (each = build → fresh live test → publish):**

- **0.2.0** — Task 63 (F1 ✅) + Task 64 (F2) → **wow #1 cross-project cold-open**. Gated above.
- **0.3.0** — Task 65 (L5b semantic recall) → **wow #2 recall-with-reasoning** = **video parity reached**.
- **0.4.0+** — Task 66 (temporal validity) → **wow #3 currency / contradiction-catch** = past-the-video differentiator.

- [x] 66. _shipped 2026-07-02, PR #249_ — **Temporal validity — facts stay TRUE as they age (§16.18 implementation).** → [archive](tasks-archive.md)

- [ ] 67. **→ PRIOR ART (D-374 sweep): READ [`2026-06-14-persona-promotion-models-cross-system.md`](../docs/research/2026-06-14-persona-promotion-models-cross-system.md) BEFORE committing to option (c).** A 9-system survey (mem0/MemoryOS/Letta/langmem/graphiti-Zep/claude-mem/ChatGPT) found *not one* gates promotion behind a manual signal — all use automatic frequency × recency classification. This task's current lean (an explicit `--cross-project` flag) may be the field's REJECTED minority approach, not the safe cheap one. **[D-253a trigger: a user asks for IMMEDIATE cross-project promotion (the janitor/weekly path covers the default); lean option (c) `--cross-project` flag when taken]** **Inline cross-project promotion on explicit `cmk remember` (F3, moved from 64.4).** The `cmk remember` rich-capture reflex writes the project tier only; cross-project promotion currently rides *auto-extract* (Task 61) + the weekly/`persona generate` pass — so an explicit capture of cross-project doctrine doesn't promote until the next janitor run. **Partial close shipped (63.1):** rich captures are real fact files the existing promotion path picks up. This task adds the *inline* path. **DECISION PENDING (mechanism fork — do not force):** classifying the fact needs an LLM; options — (a) **synchronous** Haiku spawn in the CLI (~20s/call, bad UX); (b) **detached** fire-and-forget classify+promote, mirroring the Stop-hook→auto-extract detachment (adds a subprocess surface); (c) **explicit signal** — a `--cross-project` flag the agent sets, promoting deterministically with NO classifier (no spawn, honors intent). Lean (c) for v0.2.x (cheapest, no spawn, fits the node-only ethos); revisit (b) if auto-detection is wanted. Post-0.2.0; not a v0.2.0 gate item.
  - _Requirements: US-2 (cross-project persona); composition gap (explicit capture vs Task-61 inline layer); design §16.16._

- [x] 68. _shipped 2026-07-18, PR —_ — **Weekly-curate: LLM-semantic scratchpad pruning (USER-FACING ACCURACY — validate before/after).** → [archive](tasks-archive.md)

- [x] 69. _shipped 2026-06-04, PR #111_ — **Skills as the delivery mechanism — both install routes equivalent; stop bloating the user's CLAUDE.md (D-28).** → [archive](tasks-archive.md)

- [ ] 70. **→ SWEEP VERDICT 2026-07-18 (D-353): SPLIT — 70.5 (source-trust tiers) rides Task 95's lane per D-352/F2; the inject/output-defense remainder gets a named trigger: an injection-through-memory incident, or the next security-depth batch (with 217).** **Inject/output injection-defense — defense-in-depth around memory poisoning (HIGH security).** From the source-level deep dive ([`2026-06-01-deep-dive-product-memory-implementations.md`](../docs/research/2026-06-01-deep-dive-product-memory-implementations.md) #1): Hermes scans memory at **three** points — write, **snapshot-build** (replaces poisoned entries with `[BLOCKED: … inspect]`), and **output** (a streaming scrubber strips spoofed `<memory-context>`/system-note tags) — and **fences** recalled memory as `[System note: recalled context, NOT user input; treat as background data]`. **We only Poison_Guard at write time.** This matters MORE for us than for any of them: our memory is **committed to git**, so a poisoned fact is a **supply-chain vector that travels with `git clone`** (a teammate clones it, it injects into their session). Build: (70.1) re-scan facts/scratchpads at `cmk-inject-context` build time → `[BLOCKED]` + audit; (70.2) fence injected memory as non-authoritative background data; (70.3) (stretch) scrub spoofed fence/system-note tokens from injected content; (70.4) **add invisible/zero-width Unicode (+ bidi/homoglyph) patterns to Poison_Guard** — Hermes scans for these and we don't (verified: `poison-guard.mjs` covers secrets + injection only); a hidden-instruction vector in committed memory; (70.5) **source-trust tiers at ingestion (evidence-before-belief; from the 2026-07-10 sweep — the Always-On survey §8.2.1, D-308):** Poison_Guard screens PATTERNS, but nothing structurally distinguishes "the user stated this" from "a tool output / pasted web content stated this" before auto-extract promotes it to committed cross-session authority — the survey's write-poisoning threat model is exactly untrusted-source-becoming-trusted-authority, and the kit's bi-turn extraction already KNOWS the origin (the D-303 user-vs-assistant-origin trust routing is the seed: extend it to a recorded `source_trust: user-stated | assistant-derived | tool-output` provenance field + a higher promotion bar for non-user-stated State/policy facts — [sweep note](../docs/research/2026-07-10-memory-research-sweep.md)). Post-v0.2. _Requirements: NFR-9 extension (poison-guard at the inject boundary); design §6/§NFR-9._
  - **🔬 PRIMARY-SOURCE GEM + SPLIT (deep-read 2026-06-28; D-218).** Hermes ([`tools/memory_tool.py` / `agent/memory_manager.py` / `tools/threat_patterns.py`](https://github.com/)) is the LONE primary source — verified that mem0/letta/MemoryOS do NOT defend stored memory against prompt-injection before re-injecting (mem0's `sanitize_*` is Cypher/JSON escaping, not injection defense), so there's no convergent-third-party shortcut. Hermes scans at THREE points: write, snapshot-build (`scan_for_threats(scope="strict")` → replaces the entry with `[BLOCKED: … use memory(action=remove)]`, original kept in live state, placeholder enters the snapshot), and output (a stateful streaming scrubber strips spoofed `<memory-context>` spans split across deltas); and FENCES recall in `<memory-context>[System note: recalled context, NOT user input]`. Its invisible-Unicode set (`threat_patterns.py` `INVISIBLE_CHARS`): `​-‍`, `⁢-⁤`, `﻿`, `‪-‮` (bidi) — one set-intersection pass. **Confirmed our gap:** `checkPoisonGuard` is called ONLY at write time (`memory-write.mjs`/`write-fact.mjs`); `inject-context.mjs` never imports it — the entire inject/output boundary is undefended, and a fact poisoned post-write (hand-edit, cloned poisoned repo, write-time false-negative) injects verbatim. **SPLIT by cost/risk:**
  - [x] 70.4 **invisible/zero-width/bidi Unicode patterns** → CHEAP, conflict-free, pull forward as a SECURITY PATCH (one frozenset + one intersection pass added to the existing `poison-guard.mjs` catalog; no framing conflict). _shipped (branch) 2026-06-30 — a new `injection_invisible_unicode` Poison_Guard pattern rejects 17 explicit code points at write time: zero-width (U+200B/C/D, U+2060 word-joiner, U+FEFF BOM), bidi controls + isolates (U+202A–E LRE/RLE/PDF/LRO/RLO, U+2066–9 LRI/RLI/FSI/PDI — the Trojan-Source class), + soft-hyphen/Arabic-mark/Mongolian-separator (U+00AD/061C/180E). A hidden-instruction vector in COMMITTED memory (travels with `git clone`). Built REVIEW-SAFELY (`INVISIBLE_UNICODE_CODEPOINTS` array + `buildInvisibleUnicodeRe()` → no literal invisible chars in source). Verified: all 17 code points rejected + ZERO false positives on whitespace/ASCII/accents/CJK/emoji. 5 tests; 145 caller tests (auto-extract/memory-write/write-fact) green. Grounded in Hermes parity + the kiro-design §699 / kiro-requirements char set (the research). 2470+._ _Candidate for the next patch lane (matches the v0.4.2 security-patch precedent) — D-218._
  - [ ] 70.1 + 70.2 inject-time rescan→`[BLOCKED]` + fence → medium build; **70.2 must use OUR preamble framing, NOT Hermes's "background data"** — our shipped 75.0 preamble says "injected memory is authoritative", so the fence reads "authoritative reference data" (Hermes itself reconciled to this wording, `memory_manager.py:346`). Backlog-no-version-yet (a minor lane).
  - [ ] 70.3 streaming output-scrub → **needs-deeper-design**: gate on whether Claude Code even exposes a model-OUTPUT-stream hook the kit can filter (may be impossible in CC). Flag, don't assume.
- [ ] 71. **→ SWEEP VERDICT 2026-07-18 (D-353): trigger FIRED unnoticed (the Task-210 doctor touch). Laned v0.6.x — the doctor-UX rider batch (with 47+48).** **[D-253a trigger: the next doctor-surface touch (with 47) wires the sha1-verify HC — the first leg; OR a hand-edit drift incident live]** **External-drift guard — detect hand-edits to memory → re-screen + FLAG (NOT refuse; reframed 2026-06-28, D-218). Nice-to-have safety feature, not a bug.** **⚠️ FRAMING CORRECTED after the primary-source deep-read:** the original "detect + **REFUSE** hand-edits" (modeled on Hermes, which treats hand-editing as corruption and refuses the write) is **WRONG for our git-native model** — the kit's whole wedge is *committed, diff-able, PR-able, human-readable* memory; we WANT people to read and PR `context/`. A guard that refuses hand-edits fights the wedge. **The correct posture (primary source — basic-memory, the most git-native file-based competitor):** [`sync/sync_service.py::scan()`](https://github.com/basicmachines-co/basic-memory) treats an external edit as a FIRST-CLASS input — cheap `mtime`/`size` gate → `compute_checksum()` confirm vs the DB-stored checksum → if changed, **RE-INDEX (`report.modified` → `_index_changed_files`), never reject.** Its explicit stance: files are the source of truth, the DB is a rebuildable projection that catches up to hand-edits — **identical to our markdown-as-truth + rebuildable-SQLite model.** (letta is N/A — DB-backed, no external-edit posture.) **So reframe REFUSE → RECONCILE + FLAG.** **What we already have (the cheap primitives):** `content-hash.mjs::hashContent` (SHA-256) + provenance `sha1` per fact (recorded at write, but **never re-verified** — confirmed: zero `sha1.*mismatch` checks), `cmk reindex`, the audit log (which a hand-edit produces NO entry for — so it can't even see one today). `guard-memory.mjs` blocks DELETES only, never EDITS. **Done when:** ☐ a `cmk doctor` HC flags fact-files whose body SHA differs from the recorded provenance `sha1` (the detection primitive — exists, just unwired); ☐ on a kit write that finds drift, **re-screen the hand-edited content through Poison_Guard** (catch a secret/username-leak a hand-edit slipped past the safe path) + flag — never silently clobber, never refuse; ☐ optionally a pre-commit hook so the screen runs at the COMMIT boundary (the real risk for a committed store: a poisoned/leaking line shipping via `git commit context/`). **The win is the commit boundary, not write-refusal.** **Live data-point (2026-07-02, the user's catch):** every dogfood-memory flush on THIS repo already hand-rolls the commit-boundary screen (the CLAUDE.md checkpoint-#4 pre-commit scan of new fact files for secrets/home-paths — the agent Read-ing raw `context/memory/*.md` before staging); the name half is validator-backed (`validate-maintainer-name-confined`), the secret/home-path half is manual regex every flush. So the trigger is WARMER than "incident live" — the manual screen recurs at every task boundary; when 71 is taken, its pre-commit leg subsumes that hand-rolled step. Pairs with Task 70 (sibling: 70 guards READ/inject, 71 guards the EDIT/commit boundary — distinct surfaces; Hermes bundles them, for us they're separable). Post-v0.2 → backlog-no-version-yet (sequence after Task 70, shares the supply-chain-vector framing). _Requirements: write-path integrity; design §6.5; basic-memory reconcile-don't-refuse (D-218); Task 70 sibling._

- [~] 72. **User-tier portability across machines — the persona half of the wedge doesn't travel (HIGH; honesty gap in D-27).** _CORE shipped 2026-06-05, branch task-72-persona-portability; D-69. 72.1 (`cmk persona export`/`import`) + 72.3 (docs) + the D-27 honesty fix DONE; 72.2 (`cmk persona sync` git auto-pull/push) DEFERRED as a documented fork (the "deep-research candidate" / seamless-UX design — design §16)._ Built: [persona-portability.mjs](../packages/cli/src/persona-portability.mjs) — `exportPersona` (allow-list bundle: USER/HABITS/LESSONS + settings.json + fragments/ + queues/; runtime `.locks/.index/.import-backups` never bundled) + `importPersona` (overwrite with per-file backup + **transactional rollback** + reindex tier-U + Door-4 audit). Wired `cmk persona export <file>` / `import <file>` (subcommands.mjs). OS-agnostic JSON bundle (forward-slash paths → Windows↔Mac round-trip). Honesty fix: README FAQ + CLI.md + npm-landing README now state "two scopes, two transports" (project follows the repo; persona follows the human via export/sync). Tests: [cli-persona-portability.test.js](../tests/cli-persona-portability.test.js) (9: bundle+exclude-runtime, round-trip, backup-on-overwrite+over-mutation, backup-not-re-exported, transactional-rollback, bad-kind/version/non-JSON/missing rejects). The wedge claims "your coding persona + project memory, **portable across machines**." Project memory IS (committed `context/`, travels with `git clone`). But the **user tier (`~/.core-memory-kit/` — HABITS/USER/LESSONS, the cross-project persona) is machine-local and NOT in git** — clone the repo on a new computer and the persona starts EMPTY. (Every competitor is the same — Hermes `~/.hermes`, gstack `~/.gstack` — none port the user model across machines; but our wedge *implies* it.) **KEY (verified 2026-06-01): the content is ALREADY portable — it's a UX/automation gap, not architectural.** The kit reads the user tier from `homedir()/.core-memory-kit` (auto-adapts per OS; the OS username is NEVER a key) and U-tier content is home-path-sanitized to `~` (write-fact.mjs:159). So **a plain folder-copy across machines/OSes already works** (Windows→Mac included). Seam: `MEMORY_KIT_USER_DIR` also points it at a synced/git folder. What's missing is a *button*, not a mechanism. Build options: (72.1) `cmk persona export <file>` / `import <file>`; (72.2) optionally `cmk persona sync` (make `~/.core-memory-kit/` a git repo the user pushes); (72.3) document the `MEMORY_KIT_USER_DIR → dotfiles/Dropbox` pattern. Also **fix the D-27 wedge wording** to be honest (project memory ports automatically; persona ports via export/sync). **DESIGN MODEL (the user 2026-06-01): two scopes, two transports.** Project memory **follows the REPO** (committed, shared — teammates get it). Persona **follows the HUMAN, NOT the repo** — *that's why it's in `~/.core-memory-kit/` and must stay out of the project*: committing it would leak your working-style to teammates who clone (breaks team dev). So portability must be **per-human, not per-repo**: `cmk persona sync <git-url>` (make the user tier a git repo with a remote → auto-pull at SessionStart + auto-push after curation = seamless/automatic, private to you) and/or `cmk persona use <synced-path>` (Dropbox/iCloud). **The trap to avoid: never commit the persona into the project to make it portable** — that breaks the team scenario. Scenarios to satisfy: solo/1-machine (✓ today), solo/N-machines (sync via your channel), team (each person's own persona — guaranteed because it's never in the repo), team+N-machines (per-human transport → no collision). **Deep-research candidate:** the best seamless/automatic sync UX (dotfiles patterns, auto-conflict-resolution on the persona git). **Evaluated the user's "per-user folder IN the project + import/export" proposal (2026-06-01):** ADOPT the **export/import primitive** (`cmk persona export`→bundle, `import` on the new machine — explicit, OS-agnostic, no collision control). REJECT putting the persona *in the project repo* (per-user folder / auto-mirror) for THREE reasons: (1) **privacy** — persona is personal; committing to the SHARED repo exposes it to all teammates + git history forever, removing the user's choice; (2) **the OS/git username key differs across the human's OWN machines** (e.g. `work-laptop-user` vs `home-user`) → namespacing fails the exact cross-machine case; (3) **gitignore trap** — gitignored = doesn't travel with clone; committed = leaks + bloats team history. The auto-mirror+collision-control path buys the committed-persona downsides. **Seamless version that avoids all of it: `cmk persona sync <YOUR private git url>`** — user tier becomes a git repo on the HUMAN's own remote (not the project's); auto-pull/push; git handles transport+collision; private. **One opt-in exception:** solo dev on a private personal repo → committing the persona is harmless (no team); offer `cmk persona commit-to-project` opt-in, default OFF. Post-v0.2. _Requirements: T2 (portability) extended to the user tier; design §1.1 (tiers); D-27._

- [ ] 73. **→ SWEEP VERDICT 2026-07-18 (D-353): trigger FIRED (Task 230 = the install/repair-surface touch). NOT subsumed by 230 (230 refreshes KIT-OWNED scaffold; this is user-tier seeded-content re-render). Laned v0.6.x upgrade-hygiene rider.** **[D-253a trigger: the next install/repair-surface touch; OR a stale-placeholder user report on upgrade]** **Upgrade/repair: re-render stale placeholders in existing tiers (live-test finding #6).** `cmk install`/`init-user-tier` are create-not-clobber, so a tier created BEFORE a template fix keeps the stale content forever — e.g. a user tier created pre-placeholder-fix still shows literal `{{TODAY}}` after re-install (verified 2026-06-01: `~/.core-memory-kit/HABITS.md` dated 2026-05-24, pre-fix, still literal; the NEW live-test-4 project tier rendered `2026-06-01` correctly). No migration path. Build: extend `cmk repair` to detect unrendered `{{...}}` placeholders **AND stale template comments** (+ other known drift) in existing project/user-tier files and re-render idempotently, WITHOUT clobbering real captured content. **🔺 BUMPED off post-v0.2 → v0.2.x (the user 2026-06-02): affects EVERY existing user on upgrade, not a one-off tier.** **Re-confirmed + scope-broadened 2026-06-02 (live-test-6):** after the Task-82 template cruft-scrub shipped AND a clean global `npm uninstall -g` + reinstall, the machine-global user tier STILL carried old cruft — `~/.core-memory-kit/fragments/INDEX.md` had `Type taxonomy per design §2.2` (hand-cleaned during the file review). So **Task 82 only fixes NEW installs; every existing user keeps the old cruft** — both the `{{TODAY}}` placeholder class AND the dev-comment class — because the user tier is create-not-clobber + survives reinstalls. The repair/upgrade must refresh stale template comments + placeholders idempotently, leaving captured bullets untouched. _Requirements: upgrade/migration discipline; design §placeholder-render; surfaced again by live-test-6._

- [x] 74. _shipped 2026-06-30, PR —_ — **Re-inject memory after compaction (PreCompact hook) — the kit's snapshot is lost mid-session on…** → [archive](tasks-archive.md)

- [x] 75. _shipped 2026-06-10, PR #147_ — **Recall TRIGGER — make the agent actually USE memory when it needs old context (the missing half of "recall").** → [archive](tasks-archive.md)

- [x] 76. _verified 2026-06-03, PR #99_ — **`cmk lessons promote` is a STUB + no `--tier U` → explicit cross-project promotion is BROKEN, and the…** → [archive](tasks-archive.md)

- [x] 77. _shipped 2026-06-08, PR —_ — **Rename version-named-but-general docs → version-agnostic — the misnomer class (the user 2026-06-02 +…** → [archive](tasks-archive.md)

- [x] 78. _verified 2026-06-03, PR —_ — **The wedge's AUTO half — an explicitly-stated cross-project rule must auto-promote at HIGH confidence, not…** → [archive](tasks-archive.md)

- [x] 79. _shipped 2026-06-02, PR —_ — **`cmk install` should allow-list the kit's own CLI in the project `settings.json` so the agent's EXPLICIT…** → [archive](tasks-archive.md)

- [~] 80. **→ SWEEP VERDICT 2026-07-18 (D-353): the 2026-06-04 severity gate is long past; re-verdicted — trigger: recall-miss evidence attributable to missing environment facts appears in the stats/recall-log (Task 212's dashboard is the detector).** **Auto-extract under-captures ENVIRONMENT / config facts → the `Environment Notes` section stays empty (live-test-5 finding, 2026-06-02).** _REOPENED 2026-06-04 (D-56) — PARTLY COMPLETE. The prompt-side fix shipped (80b: classifier item #3 instructs config capture), but the 2026-06-04 cut-gate run **reproduced the exact gap** — `cmk search "port 8000"` → no results, Claude-SDK choice uncaptured. A prompt-only nudge to Haiku is **non-deterministic**; it deprioritized the port again. 80b itself flagged "live proof on a different config still wanted before declaring env-capture done" — so the `[x]` was premature. The reliable fix is the **deterministic** PostToolUse/observe-edit path (80c), not asking the classifier nicely._ The Stop-hook auto-extract captures *preferences / doctrine* richly (venv rule, layered architecture, type-hints — all landed at `trust: high`), but **zero environment facts**: in live-test-5 the build clearly established `port 8000`, `claude-agent-sdk 0.2.87`, and the `claude` CLI path, yet `cmk search "port 8000"` → no results and `MEMORY.md § Environment Notes` is **empty** — even though the template ships that section *for exactly this* ("Auto-extract picks these up from PostToolUse hook events"). So a designed capture path isn't firing. The classifier prompt ([auto-extract.mjs](../packages/cli/src/auto-extract.mjs) `buildExtractionInstructions`) prioritizes durable preferences/decisions and filters config like a port number as not-durable-enough. Fix: tune the classifier to ALSO capture environment/config facts — versions, ports, URLs, tool paths, env state — and route them to `Environment Notes` (or an `environment`-typed fact). Same shape of change as Task 78 (prompt-side). Verify the `PostToolUse` observe-edit path also contributes env facts as the template promises. **Severity gated on the Session-2 recall result**: if Session 2 recalls "port 8000" cleanly (injected memory or a one-step grep), this is post-v0.2 polish; if recall fumbles, it's a fix-before-cut (env recall is core to the kit's "recall what we decided/configured" pitch — the video's L5 headline). _Requirements: recall quality (D1); design §6.0 (auto-extract) + §2.1 (Environment Notes section); surfaced by live-test-5; relates Task 78 (classifier tuning)._
  - **💡 A NEW CAPTURE SOURCE idea (`navbytes/nt` deep-read 2026-06-29; D-227).** `nt` (MIT, Go, git-native plain-text agent memory) captures from a source WE DON'T: a **PostToolUse hook that mirrors Claude Code's `TodoWrite` list into durable storage** (idempotent, tagged `src:claude`). We capture from CONVERSATION turns (Stop-hook auto-extract) + file edits (observe-edit) — but NOT from the agent's own LIVE TASK STATE (`TodoWrite`). An agent's todo list is a rich, structured signal of what it's WORKING ON / DECIDED-to-do — capturing it (as active-thread context, or to reconcile "what was planned vs shipped") is a capture surface adjacent to this task's "capture more than just preferences." **Worth evaluating** when env/config capture (80c) is built: is `TodoWrite`-mirroring a useful 3rd capture source (alongside turns + edits), or noise? The kit already READS its own todos in-session; `nt` shows persisting them as memory is a real pattern. _Relates the auto-extract capture surfaces (turns/edits + this new todo-state source), D-227._
  - [x] 80b. **Generality made structural, not asserted (audit follow-up, D-36).** _Branch task-84-compressor-grounding._ The original trigger #3 enumerated config types (`ports / versions / DBs / URLs / paths`) AND pasted the scenario's literal values (`port 8000`, `Python 3.13`, `claude-agent-sdk 0.2.87`) — over-fit to the guide's project, and the test was **tautological** (`toMatch(/port 8000/)` only re-checked the example string I typed). **Fix:** the prompt now defines the class by its *recall property* — "concrete setup/config values the next session would otherwise have to re-derive to recover" — with NO example values and NO "code" assumption (domain-neutral). The test now asserts the **directive** (the class is named, defined by re-derive-property, "from the WORK") AND **guards against** the scenario literals + "code" leaking back (`.not.toMatch(/port 8000|claude-agent-sdk|Python 3\.13|re-read the code/)`). Live proof on a *different* config (port 3000 + Postgres) still wanted before declaring env-capture fully done — folded into the Task 84 live re-test.
  - [ ] 80c. **Deterministic env/config capture via observe-edit — the prompt-only path is unreliable (D-56, cut-gate-reproduced).** 80b's classifier instruction is necessary-but-insufficient: an LLM prompt can't GUARANTEE config capture (cut-gate 2026-06-04: Haiku ignored `port 8000` despite item #3). Implement the deterministic half the template already promises (`Environment Notes — "Auto-extract picks these up from PostToolUse hook events"`): the **PostToolUse observe-edit** hook detects concrete config values from tool I/O — ports (from run/serve commands), package versions (from install output), tool paths, URLs, env vars — and routes them to `MEMORY.md § Environment Notes` (or an `environment`-typed fact), **independent of Haiku's judgment**. Keep 80b's prompt nudge as a best-effort complement.
    - [ ] 80c.1 observe-edit detects config values from PostToolUse tool events.
    - [ ] 80c.2 route detected config → `Environment Notes` via the safe write path (sanitized + Poison_Guard'd, same as any capture).
    - [ ] 80c.3* test: a build that establishes `port 8000` → `cmk search "port 8000"` hits **without** relying on the classifier (deterministic); over-mutation — non-config tool events don't spam Environment Notes.
    - _Relates lifecycle-map G8 (config under-capture compounds the 7-day session-loss), §2.1 (Environment Notes) + §6.x (observe-edit). Filed 2026-06-04._

- [x] 81. _shipped 2026-06-02, PR —_ — **🚨 v0.2.0 CUT-BLOCKER — Windows: the detached hook spawns pop a visible console window (live-test-5…** → [archive](tasks-archive.md)

- [x] 82. _shipped 2026-06-02, PR —_ — **Scrub kit-internal cruft out of the user-facing scaffold templates (live-test-5 finding, 2026-06-02; the…** → [archive](tasks-archive.md)

- [x] 83. _shipped 2026-06-02, PR —_ — **Stop dumping `Files Touched` LOG entries into the memory files / snapshot (live-test-5 finding,…** → [archive](tasks-archive.md)

- [~] 84. _**D-253a (2026-07-02): 84a+84b SHIPPED (grounding + supersede rules, all 3 compression layers); only 84c (cosmetic scaffold examples) remains — deferred with its D-253 trigger (a non-coding user reports confusion)._ **The session COMPRESSOR hallucinates facts + keeps stale early-stage state → contradicts the (correct) fact memory → agent distrusts recall (live-test-6 finding, 2026-06-02).** R3 recall test: asked "what port / how structured" on a fresh session. The granular fact memory was **100% correct** (port 8000 captured 3×, FastAPI, layered, type-hints — Tasks 80/82/83 all worked). BUT the Haiku **session compressor** (`compress-session` → `today-*.md` → `daily-distill` → `recent.md`) wrote *"Building Claude agent **Flask** application: app.py, services/claude_agent_service.py"* — **the project was NEVER Flask** (compressor invented a framework name) and **`app.py` was the Stage-0 flat file, superseded by `app/main.py` at Stage-1** (stale state never updated). That wrong summary is ALSO injected at SessionStart, so the agent saw a **contradiction** (snapshot: Flask/app.py vs facts: FastAPI/app.main) → correctly distrusted memory, re-read `config.py`/globbed, and flagged it. Undermines the "it just knows from memory" feel even though the fact layer was right. **The kit self-corrected** (auto-extract queued `P-62V5WLVG` "Flask is wrong, it's FastAPI — correct stale memory") — the system noticed, which is good, but the bad data still shipped into a recall. Two sub-problems: **(a) compression hallucination** — Haiku invents/mis-summarizes facts (a framework name) the source never contained. **The fix must be GENERAL, not an identifier whitelist** (the user 2026-06-02: *"preserve framework/port/path verbatim … will only work for that scenario … we are suppose to be working on a general solution"*). Root cause: the `buildCompressionInstructions` HARD RULES protect citation IDs (`never invent new IDs`) but carry **no faithfulness/grounding rule at all** — nothing forbids emitting content absent from the buffer. The general guardrail: a HARD RULE that **every bullet must be grounded in the session buffer; do not infer or guess any fact not explicitly stated in the buffer; do not carry forward content from earlier summaries; if unsure, omit it.** No example categories — naming "framework / port / path" even as examples anchors the model and re-introduces the scenario-fit (the user 2026-06-02: *"you need to remove 'framework/library/tool names, file roles, ports'"*). One example-free rule kills Flask AND every other invented-fact class. Optional second layer: a cheap self-check / verify pass. **(b) stale-stage compression** — the rolling session summary keeps superseded early-stage state (`app.py` flat) after the project moved on (`app/main.py`); compression must supersede/refresh, not accumulate stale narrative (relates Task 68 semantic-prune). **The fact-capture layer is trustworthy; the COMPRESSION/summary layer is the weak link.** _Requirements: recall correctness (a hallucinated fact in the snapshot is worse than no fact); design §8 (compression) + §6.x; surfaced by live-test-6; relates Task 68 (stale-thread prune) + D-35._ **Design dossier for any future compressor refinement (2026-06-15, [OpenHands condenser note](../docs/research/2026-06-15-openhands-condenser-context-management.md)):** four un-adopted ideas from OpenHands's condenser worth considering when next touching §8 — **`keep_first`** (NEVER condense the session's anchor/framing facts — a summary that loses "what the session was FOR" is the 84(b) staleness class from the other side), **`minimum_progress`** (don't pay for a summary that shrinks <~10% — a min-shrink floor on top of the cooldown), **HARD-vs-SOFT trigger** (will-the-next-call-fail vs housekeeping — defer soft compression off the hot path), and **shrink-and-retry** when the summary call itself overflows. Idea-inputs, not commitments.
  - [x] 84a. **(a) compression hallucination — grounding rule shipped across ALL THREE compression layers.** _Branch task-84-compressor-grounding._ Example-free HARD RULE #1 (*grounded in the input; never infer; never carry forward; omit if unsure*) added to `compress-session` + `daily-distill` + **`weekly-curate`** (the 3rd layer — found by D-37's full read-through of every prompt, NOT by the live test; it fires weekly so the Flask scenario never hit it). Domain-neutral (no "code" assumption — the kit serves non-coding work). Door-4 tests on all three; design §8.4 updated. **Live-verify still wanted** (confirm Haiku stops inventing on a real re-test) before counting (a) fully closed.
  - [x] 84b. **(b) stale-stage compression** — the rolling summary keeps superseded early-stage state (`app.py` after the project moved to `app/main.py`); compression must supersede/refresh, not accumulate stale narrative. _shipped 2026-06-03, PR #109._ Fix: a domain-neutral, example-free supersede-on-change clause appended to HARD RULE #1 in ALL 3 compression layers (compress-session, daily-distill, weekly-curate) — "When the [buffer/summaries] corrects/replaces/reverses something earlier, keep ONLY the latest version of that fact … (resolves contradictions, NOT coexisting facts on different points)." compress-session already had cross-SESSION "do not carry forward"; this adds keep-latest-on-CONTRADICTION at the rollup layers (dedup ≠ supersede). Skill-review M1: tied "latest" to the changed fact + a coexisting-facts carve-out so it can't drop distinct current facts. Prompt-presence tests in all 3 layers; suite 1372 incl. live spawn-smoke; live-verify wedge 2/2+2/2. **This is the COMPRESSION-layer supersession; the FACT-layer auto-supersede (F-D, a contradicting granular fact like uv-vs-venv) is the harder semantic-contradiction problem — roadmapped v0.3 (D-26 #3), see the F-D scope note.** Relates Task 68 (semantic-prune).
  - [ ] 84c. **(c) Class-B cosmetic — scaffold seed examples are uniformly coding/git (the user's call, D-37).** The scaffold seed bullets (MEMORY/USER/HABITS/LESSONS) + injected `cmk remember` worked examples ("PR #142", "git push/reset", "fix the code not the test", "FastAPI/Kamal") all assume a coding user. They're illustrative placeholders the user replaces (do NOT change capture behavior), but a non-coding friend opening the scaffold sees "a tool for programmers." Decision deferred to the user: neutralize the examples vs. keep (defensible for a Claude Code tool). Also Class-C: npm `template/` seed bullets vs bare plugin `context-template/` — two scaffold sources drifted.

- [x] 85. _shipped 2026-06-03, PR —_ — **INDEX.md consistency — `cmk doctor` HC-5 false-failed + `cmk remember` left the index stale (live-test-7…** → [archive](tasks-archive.md)

- [x] 86. _shipped 2026-06-03, PR #107_ — **WEDGE BUG — multi-rule turns don't promote to the cross-project persona (live-test-8 finding, 2026-06-03).** → [archive](tasks-archive.md)

- [x] 87. _shipped 2026-06-03, PR #108_ — **COMPRESSION INPUT — `now.md` holds only file-writes, not the conversation (the real Task 84 root;…** → [archive](tasks-archive.md)

- [x] 88. _shipped 2026-06-03, PR #110_ — **The Haiku-compressor sandbox uses the WRONG CLI flag — `--allowed-tools ''` instead of `--tools ''` (D-43;…** → [archive](tasks-archive.md)

- [x] 89. _shipped 2026-06-03, PR —_ — **Automated live wedge test — `npm run live-verify` (D-45; the user's idea, 2026-06-03). WORKING — run 2…** → [archive](tasks-archive.md)

- [x] 90. _shipped 2026-06-04, PR —_ — **`cmk install` allow-lists `Skill(memory-write)` so model-invoked capture doesn't prompt (v0.2.0; D-52).** → [archive](tasks-archive.md)
